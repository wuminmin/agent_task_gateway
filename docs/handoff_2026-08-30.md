# Handoff 2026-08-30 (loop: paper revision ↔ independent TKDE review)

## 接着做这个（2026-08-30 22:20 CST 最终）
**循环已停，等作者裁决**（台账 P7-LOOP-ROUND-17）。稿件在分支 tip（12 页，`make paper-final-check` 通过）。作者决定后：若投入某项新证据，按 open-items 的条目做；若不投入，按 round-17 M8 做最后一次文本整理（主张—证据表、记号统一、传记）并交作者定稿。

## 旧的接着做（21:00 CST）
0. 链 #6（8ff7fb4，ProvSQL 叶级交叉核对宏）落地后：`bash ~/stage-e/commit-evidence.sh` → rebase/push → `make paper-final-check` → 页数回 12 → round-17 审稿。第 11–16 轮均 Major revision，剩余 blocking 为 campaign/实现级或作者亲手项（见台账 P7-LOOP-ROUND-16 与 open-items）。

1. 读 `tmp/reports/reviews/round-11/review.md`（若尚无，round-11 审稿子代理可能仍在跑或被中断：按 `docs/agents/paper-review-loop.md` 的固定指令重起，输入在 `round-11/{main.txt,supplement.txt,evidence.tex}`）。
2. 按意见分 text-only / needs-new-evidence；text-only 直接改并重建（wt-review 配方见 memory `review-worktree-build-order`；主文必须 12 页，`pdftotext -f 13 -l 13 main.pdf - | wc -l` 为 0）。
3. 停止条件：独立审稿 Verdict 为 Accept 或 Minor revision → `catm-notify done` 向作者报告（作者原话：continue until paper passed … report to me when passed）。

## 当前状态（2026-08-30 19:20 CST; 早先状态见 git 历史）
- 分支 `tkde-review-revisions` @ 57e63b7。`make paper-final-check` 在 ff9190d 通过：main 12 页 / supplement 22 页。round-11/12/13 均 Major revision，每轮 text-only 项已全部处理；round-14 审稿进行中（`tmp/reports/reviews/round-14/`）。
- 证据链：链 #5 在 5f8f207 绿（早先 #2 00acf61、#3 f359459、#4 865621f）（DSN 4,386 测试受理；v5-outcome 重建 rc=0），证据提交 10e5717。`\EvidenceChainCommit` = 00acf61c977a。
- 新证据（本日）：随机化根家族结算测试（24 seeds / 693 结算 / 67 收窄子任务，进 v5 原始日志）；SSB 可降低性 0/13→2/13；LLM 助手写的 40 条报表 SQL 原样 18/40 可降低（evaluation/agentworkload）；封存 Scale/ProvSQL 逐成员语义集合链接（7,560 + 270）；子相位 pilot-06+07 每格 4 样本；生成计划差分 campaign 5,000 计划 0 不一致；TPC-H 可降低性 0/22。
- 台账最新行：`docs/codex_publication_execution_plan.md` 末尾 P7-LOOP-* 系列（含两次自错：磁盘填满、wire/schema 未同步）。

## 仍需新 campaign 类（作者授权下自行裁决，尚未做）
- 先验校准的 bounded arm + 实跑 counter arms：`evaluation/finalv5rls/corpus.go:397` 硬编码 70% floors；需新 Catalog profile（config/catalog.yaml 是受测路径→链）、adapter/validation 新 mode、registry/coverage 新格；估 3–5 h + pilot 15 min + 链 55 min。
- ProvSQL 电路叶级 F_D 交叉核对：adapter 只 drain 到每输出行根（`evaluation/cmd/final-v5-adapter/provsql.go:869`）。
- >2 源 join E2E 格：S4 depth-4 仍是两源。

## 环境注意
- WSL 根盘 1 TB 余 58 G：retention 用 mv 不 cp（memory `wsl-disk-budget`）。
- 改 `evaluation/internal/experiment` wire 结构后立刻跑 `go test ./evaluation/internal/experiment -run Schema`（memory `sample-wire-schema-sync`）。
- `paper/tkde/generate_evidence.py` 是受测路径：任何改动都要重跑链（`CHAIN_ALLOW_RED_UNITTESTS=1 bash ~/stage-e/launch-chain.sh`，红单测仅允许是 raw-command 不匹配那 5 个）；链后 `bash ~/stage-e/commit-evidence.sh`。
- 生成器返回值现在会给文档加 `family_property` 键（测试侧已适配）；下次必须改生成器时顺手改为不污染文档。
