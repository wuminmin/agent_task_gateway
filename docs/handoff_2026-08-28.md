# Handoff 2026-08-29 03:25 CST（campaign 已封存；门禁复跑与 v5-outcome 重建进行中）

## 接着做这个

1. **看主工作树门禁结果** `~/stage-e/logs/gate-main.out`（03:11 CST 起，HEAD `1b62d44`，umask 022）。
   通过（末行 `GATE_EXIT=0` 且日志内无 `integration test failed`——两者都要看）后：
2. **v5-outcome 重建**：`~/stage-e/v5-outcome-rebuild.sh`（raw 测试 + schema-2 清单 → 生成器 draft → `record-compose-e2e.sh`
   再跑一遍 compose 套件并升 schema 3 + `--evidence-mode final`）。完成后提交 `evaluation/v5-outcome/{evidence.json,compose-receipt.json,raw/}`
   与 `paper/tkde/generated/evidence.tex`，`make paper-final-check`（需净树）。
3. 正文改动：`tmp/reports/p71-prose-draft.md` 待作者过目后改 `main.tex`/`supplement.tex`（P7.6 作者逐字节审批）。
4. 归档：P77 旧归档已被作者迁盘时删除（非论文证据）。188G 旧 pilot 目录暂留 WSL 盘；**等作者回到 NAS 局域网后**经内网迁到 NAS `~/taskgate-evidence/archived-campaigns/`（Tailscale 中继实测 1.6 MB/s 不可行）。
5. `tmp/reports/decision-38-draft.md` 待作者确认。台账草稿尾部（P92-SELF-5 / LINE3-GATE / UMASK / ADAPTER-ALIGN / P71-WIRING）
   在下次 docs 提交并入正式台账。
6. P7.2–P7.9。

## 当前状态（全实测，2026-08-29 03:25 CST）

- git：origin=NAS=`1b62d44`；WSL 主工作树 `1b62d44`（门禁跑中，勿 pull）。今日提交链：`e83df34`(finalizer module_proxy) →
  `32c9047`/`78824df`(台账/handoff) → `e8d0d28`(P7.1 清单+宏) → `7f4e81a`(line3 ff 合并) → `20dd831`(run-04 重钉 + concurrency 宽度)
  → `572234e`/`d7af6c5`/`1b62d44`(adapter scale-extreme/测试期望/vet/gofmt)。
- **formal-v111-publication-03 封存**：campaign-evidence sha `45e5eacd…`，9393 样本全 pass，172 格。
- run-04 三测在真 PG 上自洽通过（新钉 `a10c9854`）；adapter 包 ok；finalv5publication/publication-review 在 644/755 树上 ok。
- WSL 文件模式已归一（104 个 664 文件 → 644）；ssh umask 0002 是根因，包装脚本已加 `umask 022`（见 docs/agents/environment.md）。

## 教训（本会话新增）

- `-root` + 绝对路径：route-matrix 用 resolvePath 兼容，cache-isolation-live 用 filepath.Join 不兼容——改一处必须 grep
  live.sh 全部 `-root` 调用并核对各工具实现（同类缺陷第二例）。
- 失败路径先 `docker logs` 落盘再 `down`：e2.sh 首跑的 trap 把 installer 日志清掉，多跑一次复现才拿到直接证据。
- NAS 无 pgrep；bracket-grep 会匹配到 Claude 自己的 wrapper shell——用 pidfile 或按 argv 字段过滤。
- 看守从别的 cwd 起会落到另一 CATM 会话：`catm-notify say --workspace <repo>`。
