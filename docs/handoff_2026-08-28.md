# Handoff 2026-08-28 07:15 CST（WSL 已重启核灾全绿；正式 campaign -02 运行中）

## 接着做这个

**07:06 作者已重启 WSL，07:07–07:10 核灾全绿（详见台账草稿 P92-V111-FORMAL-02），07:11:33 已发射 `formal-v111-publication-02`（看守 NAS pid 见 `~/stage-e-nas/watch-formal-v111-publication-02.pid`）。现在从第 3 步起：看着 -02 跑完；campaign 期间不 push、不动 WSL、不跑重活。**

0. （已完成）先看作者是否已重启 WSL（`ssh` 通且 `grep " / " /proc/mounts` 不含 `emergency_ro`）。未重启则只能等：
   catm 已于 02:1x 上报第二类（需作者 `wsl --shutdown` 后重开 Ubuntu-22.04）。
1. **重启后逐项核灾**（网络变了/盘坏了都要重核，别信旧假设）：
   - `dmesg | grep -iE "ext4|I/O error|sdc"`、`/proc/mounts` 根 rw；`df -h /`。
   - worktree：`cd ~/worktrees/agent_task_gateway && git fsck --no-dangling && git status`；坏了就
     `git clone` origin 到新目录（origin=`649eadb` 完整）；NAS 本地领先 2 提交（handoff、validator）待 push。
   - 私有材料：`sha256sum ~/.taskgate-signing-backup-p45/current-dataset-binding/publication-binding.json`
     应为 `3bb2771fa07b3cd7b0e0d806cf84af41d05628b958f425310368b854b77b7526`；`.env` 存在且可读。
     任一不可读 → 第三类（作者重签/重配），先报再动。
   - docker：`docker info`、`docker images | grep -c taskgate-final-v5-gateway`；go 模块缓存
     `~/go/pkg/mod/cache/download`（本机代理源）；坏了按 AGENTS.md 网络绕法重建（慢链路）。
   - `~/stage-e/`、`~/formal-v111/` 脚本是否完好（不在 git；内容见 memory `stage-e-scripts-and-watcher`）。
2. **正式 campaign 整轮重跑** `formal-v111-publication-02`（launcher 不能续跑；-01 目录不可读、作废）：
   `TASKGATE_CAMPAIGN_ID=formal-v111-publication-02 ~/formal-v111/run.sh`，前置：HEAD==origin、树净、0 容器、代理在线。
   发射后起 NAS 看守 `~/stage-e-nas/watch-campaign-nas.sh formal-v111-publication-02` 并 catm。
   **估时按实测**：轻 profile 每部署 ~12 分（与 v1.10 formal-03 一致）；重 profile 在冻结密度下无实测（P93 试跑是 pilot 密度，不可用）。
3. campaign 期间：不 push、不动 WSL 树、不在 WSL 跑重活。结束后：并入台账草稿（含 INCIDENT 三行、SELF-4）、push、
   run-04 重钉、line3 合并（scratch worktree 已 rebase 到 `c371207`）、P7.1 manifest + `generate_evidence.py` 接线。

## 事故记录（02:07–02:20 CST）

- 02:02 看守正常；02:07:30 看守记 launcher=0 无 CAMPAIGN_EXIT；02:10 实查根 fs `emergency_ro`，写入 EROFS，
  `awk/sort/scp/journalctl` 二进制 I/O error，样本文件数据块 `error reading`；docker 无响应。
- -01 进度：7 部署全过 0 失败（analytics-orders ×3 @540、lineitem ×3 @300、attack-expense-detail/001），
  死于 attack-expense-detail/002（~01:47 起）。run.out 已抢救到 NAS `~/stage-e-nas/incident-20260828/run.out`。
- 私有材料复制到 NAS 被策略拦下（未绕过）；已请作者从 Windows `\wsl$` 自备份。

## 当前状态（全实测，2026-08-28 00:30 CST）

- git：origin=WSL=`649eadb`（172 校验端修正）；NAS 本地领先一个 handoff 提交，campaign 结束后再 push。
- v1.11 链：固定点 `df574e9` → 台账 `5ef1edf` → qualification 四件套 ×3 `d45d1bc` → 台账 `13d1e17` → `649eadb`。
- 正式 campaign `formal-v111-publication-01`：plan pass（125+36+11=172，ready 11/11，3 轮），formal image
  `sha256:ae771304…`，00:27 起第一个部署 analytics-orders/001（18 格）。
- ATB：358/800 pass、37/49 skip、3 FAIL（run-04 钉旧摘要，非 campaign 输入链）。

## 教训（本会话新增）

- `-root` + 绝对路径：route-matrix 用 resolvePath 兼容，cache-isolation-live 用 filepath.Join 不兼容——改一处必须 grep
  live.sh 全部 `-root` 调用并核对各工具实现（同类缺陷第二例）。
- 失败路径先 `docker logs` 落盘再 `down`：e2.sh 首跑的 trap 把 installer 日志清掉，多跑一次复现才拿到直接证据。
- NAS 无 pgrep；bracket-grep 会匹配到 Claude 自己的 wrapper shell——用 pidfile 或按 argv 字段过滤。
- 看守从别的 cwd 起会落到另一 CATM 会话：`catm-notify say --workspace <repo>`。
