# Handoff 2026-08-28 00:30 CST（v1.11 正式 campaign 运行中）

## 接着做这个

1. **看着正式 campaign 跑完**：WSL `~/formal-v111/run.out`（launcher 输出）、
   `evaluation/final-v5-wsl2/raw/formal-v111-publication-01/deployments/<alias>/NNN/`；
   NAS 看守 `~/stage-e-nas/watch-campaign-nas.sh formal-v111-publication-01`（pid 见 `.pid`），
   叫停 `~/formal-v111/stop.sh`。**campaign 期间不 push、不动 WSL 工作树、不在 WSL 跑重活**
   （verify-build 每部署要求 HEAD==origin；正式轮在测时延）。00:25:43 发射于 `649eadb`，预计 ~5h。
2. 结束后：复核 `campaign-evidence.json`（`P30-STAGE: mechanism=pass … publication_eligible=true`、
   172 格、33 部署、失败 0），把 `tmp/reports/ledger-draft-p92.md` 尾部三行（ATB / 172 / FORMAL-01）
   与结果行并入正式台账，提交并 push，NAS/WSL 同步。
3. run-04 fixture 守卫（ATB 3 FAIL，`predicate_scope_canonical_test.go:29` 钉 `87dbe200`，现值 `a10c9854`）：
   按 line3 行做法——先核 compact==jsonb 与 source/retained AST 自洽（把常量临时改为现值跑这三测），
   自洽才重钉并在提交里写明由 E0 默认路由重投影所致；**禁止拿重钉掩盖回归**。只在 campaign 结束后于 WSL 跑。
4. line3 分支 `origin/line3-skip-gate`（`ffade18`）：E 完成后重跑 compose 门禁 → 合并。
5. 论文侧：`paper/*.py` 由 campaign-evidence 生成图表宏；P7 投稿清单。
6. `tmp/reports/decision-38-draft.md` 请作者确认后再入 `docs/final_v5_author_decisions.md`。

## 当前状态（全实测，2026-08-28 00:30 CST）

- git：origin=WSL=`649eadb`（172 校验端修正）；NAS 本地领先一个 handoff 提交，campaign 结束后再 push。
- v1.11 链：固定点 `df574e9` → 台账 `5ef1edf` → qualification 四件套 ×3 `d45d1bc` → 台账 `13d1e17` → `649eadb`。
- 正式 campaign `formal-v111-publication-01`：plan pass（125+36+11=172，ready 11/11，3 轮），formal image
  `sha256:ae771304…`，00:27 起第一个部署 analytics-orders/001（18 格）。
- ATB：358/800 pass、37/49 skip、3 FAIL（run-04 钉旧摘要，非 campaign 输入链）。

## 教训（本会话新增）

- `-root` + 绝对路径：route-matrix 用 resolvePath 兼容，cache-isolation-live 用 filepath.Join 不兼容——改一处必须 grep
  live.sh 全部 `-root` 调用并核对各工具实现（同类缺陷第二例）。
- 失败路径先 `docker logs` 落盘再 `down`：e2.sh 首跑的 trap 把 installer 日志清掉，多跑一次复现才拿到直接证据。
- NAS 无 pgrep；bracket-grep 会匹配到 Claude 自己的 wrapper shell——用 pidfile 或按 argv 字段过滤。
- 看守从别的 cwd 起会落到另一 CATM 会话：`catm-notify say --workspace <repo>`。
