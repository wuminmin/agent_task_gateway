# Handoff 2026-08-29 02:50 CST（正式 campaign 已完成并封存；进入论文接线）

## 接着做这个

1. **P7.1 论文证据接线（在 WSL 上做，raw 只在 WSL）**：写 `evaluation/final-v5-wsl2/publication-evidence-v1.json`
   （campaign `formal-v111-publication-03`，campaign-evidence sha `45e5eacd…`，submission `649eadb`，33 部署，125+36+11=172），
   把 `paper/tkde/final_v5_publication_evidence.py` 接进 `generate_evidence.py`（`\FinalVFivePublication…` 宏），
   `make paper-evidence`，改 §Evaluation/§Reproducibility 的「publication campaign has not run」段落——**正文改动作者过目（P7.6）**。
2. run-04 夹具重钉（`predicate_scope_canonical_test.go:29`，现值 `a10c9854…`，成因 E0 `900fc96` 默认路由重投影）：
   先核 compact==jsonb / source vs retained AST 自洽再重钉；`db-test-env.sh test ./evaluation/internal/experiment` 全绿再提交。
3. line3：scratch worktree 已 rebase 到 `c371207`（NAS `…/scratchpad/wt-line3`），重跑 compose 门禁 → 合并。
4. 归档：`pE-v111-activation-01`(69G)、`p93-heavy-profile-trial-01`(69G)、`formal-v5-publication-03`(50G) → D 盘归档目录
   （原 `/mnt/d/wsl-data/taskgate-evidence/archived-campaigns/` 迁盘后需重新定位；脚本 `~/stage-e/archive.sh`）。
5. `tmp/reports/decision-38-draft.md` 请作者确认后入 `docs/final_v5_author_decisions.md`。
6. P7.2–P7.9 投稿清单。

## 当前状态（全实测，2026-08-29 02:50 CST）

- git：origin=WSL=NAS=`32c9047`（含 AGENTS.md 瘦身、docs/agents/、publication 校验器、finalizer 修复 e83df34、台账 15 行）。
- **formal-v111-publication-03 封存**：`campaign-evidence.json` pass / publication_eligible / 172 格 / submission 649eadb，
  9393 测量样本全 pass（逐样本复核），非部署 16500+3240 全 pass。位置 WSL `evaluation/final-v5-wsl2/raw/formal-v111-publication-03/`（130G）。
- 收尾曾因 finalizer 严格解码不认识 `module_proxy` 失败，修 `e83df34` 后对同一批字节重跑通过（校验端漏改第 3 例）。

## 教训（本会话新增）

- `-root` + 绝对路径：route-matrix 用 resolvePath 兼容，cache-isolation-live 用 filepath.Join 不兼容——改一处必须 grep
  live.sh 全部 `-root` 调用并核对各工具实现（同类缺陷第二例）。
- 失败路径先 `docker logs` 落盘再 `down`：e2.sh 首跑的 trap 把 installer 日志清掉，多跑一次复现才拿到直接证据。
- NAS 无 pgrep；bracket-grep 会匹配到 Claude 自己的 wrapper shell——用 pidfile 或按 argv 字段过滤。
- 看守从别的 cwd 起会落到另一 CATM 会话：`catm-notify say --workspace <repo>`。
