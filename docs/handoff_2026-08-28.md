# Handoff 2026-08-28 00:05 CST（v1.11 固定点已提交；qualification 链运行中）

## 接着做这个

1. **等 WSL 上的 v1.11 qualification 链结束**（detached：`~/stage-e/qual-chain.sh`，日志
   `~/stage-e/logs/qual-chain.log`，三轮输出 `~/stage-e/logs/qual-qualification-v111-0{1,2-scale,3-provsql}.out`，
   结束标记 `CHAIN_DONE`）。**链结束前不得 push 任何提交**：`final-v5-attestation-footprint`
   (`provenance.go:119`) 与 formal build (`source.go:87`) 都要求 HEAD==origin。
2. 每轮通过（`QUAL_EXIT=0`）后：`~/stage-e/retain-qual.sh <raw/diagnosis-attestation-footprint-qualification-v111-…>`
   暂存 P48b 式四件套（marker / attestation-footprint-v2.json / postgresql-identity.json / qualification.log），
   核对 footprint 精确绑定 registry `2526b749…`；三轮合一提交 + 台账行，push，NAS 同步。
3. 跑 `evaluation/internal/experiment` 的 qualification repository guard（P61 曾在 v1.11 缺 qualification 时失败），
   `scripts/db-test-env.sh test -count=1 ./evaluation/internal/experiment`（P48b 的 ATB 先例），确认无新失败。
4. 正式 campaign（172 格 × 3 轮，约 5h，P93-TRIAL 估算）：发射前按 P77-prelaunch 盘点，
   三份 qualification 材料经 `ATTESTATION_QUALIFICATION` / `SCALE_…` / `PROVSQL_…` 传给
   `run-profile-campaign.sh`。**发射前 `catm-notify say` 通知作者**（产出论文数字，属第一类）。
5. 之后：line3 分支 `origin/line3-skip-gate`(`ffade18`) 在 E 完成后重跑门禁 → 合并；
   `tmp/reports/decision-38-draft.md` 请作者确认后再入 `docs/final_v5_author_decisions.md`（分类器拒绝 Claude 直改）。

## 当前状态（全实测，2026-08-28 00:05 CST）

- git：NAS=WSL=origin=`5ef1edf`（`df574e9` 固定点 + `5ef1edf` 台账），两树净。
- **E1-09**：live 半程完成（11/11 激活、105/105 探针 0 失败、same-query 6/6），run root
  `evaluation/final-v5-wsl2/raw/pE-v111-fixedpoint-09`。
- **E2**：两轮收敛，registry `b5b256b2→2526b749`，support 11/11，SQL 门 v1.11 真 PG16.14 通过，validate.sh EXIT=0，
  提交 `df574e9`。db-test harness 竞态根治 `755dccd`（P48b 早已观察到、当时手工绕过）。
- **qualification-v111-01**（result-heavy，默认）23:57:22 起跑于 `5ef1edf`；02 exposure-scale、03 provsql 由链自动接续。
- 台账：P92/P93 26 行已并入 `docs/codex_publication_execution_plan.md`（`5ef1edf`）。

## 教训（本会话新增）

- `-root` + 绝对路径：route-matrix 用 resolvePath 兼容，cache-isolation-live 用 filepath.Join 不兼容——改一处必须 grep
  live.sh 全部 `-root` 调用并核对各工具实现（同类缺陷第二例）。
- 失败路径先 `docker logs` 落盘再 `down`：e2.sh 首跑的 trap 把 installer 日志清掉，多跑一次复现才拿到直接证据。
- NAS 无 pgrep；bracket-grep 会匹配到 Claude 自己的 wrapper shell——用 pidfile 或按 argv 字段过滤。
- 看守从别的 cwd 起会落到另一 CATM 会话：`catm-notify say --workspace <repo>`。
