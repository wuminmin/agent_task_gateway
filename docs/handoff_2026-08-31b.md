# Handoff 2026-08-31（WSL 关机前暂停点）

## 接着做这个（重启 WSL 后，按序）
1. 重启后核灾按 `docs/agents/environment.md`（goproxy 3000 端口会死，`~/stage-e/rebind-proxy.sh`；docker 起否；df）。
2. `nohup bash ~/stage-e/live-footprint.sh > ~/stage-e/logs/live-footprint-02.log 2>&1 &`
   —— 13 profile 激活 + attest 站 + 路由矩阵 + same-query（脚本已就绪，语法查过；上次发射进到 profile_artifacts 后被关机指令终止，环境已清干净）。
3. live 绿后跑 offline half（照 `~/stage-e/offline-v112.sh` 改 R=pE-footprint-fixedpoint-01、提交信息；周期内 attest 文件随 config/profiles 一起提交）。
4. 定点提交后跑常规四门禁（DB suite / full eval——attestation 测试届时转绿 / 链 / paper-final-check）。
5. 然后阶梯 pilot：`~/stage-e/profile-pilot.sh` 改 TASKGATE_CAMPAIGN_PROFILES=footprint-bounded,footprint-unlimited（pilot 类，不需作者 binding）。

## 状态（全部已推送，tip 130d118）
- (c2) slice b 全绿：footprint 独立 experiment——证据信封+校验器+schema+adapter runner（78d8604/725434a），四门禁绿。
- slice c 已到"两 profile 注册完成"：registry/coverage/两 catalog 已提交；缺 live attest+激活支持（转绿条件）。
- 已知瞬时红：TestEveryMaterializableProfileIsAttested（live attest 落地即绿）。
- 发布类 campaign-plan 172 格分母：footprint 入 registry 后发布类规划会超 172——Phase 3 作者裁决新分母时一并改（run-nonprofile-smoke 也会受影响，pilot 类不受影响）。
- WSL 磁盘 97%（35G 空闲）；live 定点需 ~15G 瞬时。
