# Handoff 2026-08-31（P8 阶段 1 中段）

## 现状（实跑/读代码/未复核三档均标注）
- 分支 `tkde-review-revisions` 推至 bfb6a5b；WSL 主工作树、`~/wt-pilot`（subphase-pilot）、`~/wt-review` 均在该提交。
- (b) 片段扩展：代码、5,000 计划差分 0 不一致、RQ1 24/24、论文文本与补充材料已更新（main 12 页，无未定义宏）。【实跑】
- (d1) 派生优化：100k×16 扫描派生 15.67 s → 2.43 s（`TestProfileOrdinalDerivationAtScale`，同机前后对照）；digest 字节由差分测试钉住；
  `-race` 全绿；DB 套件 `./internal/...` rc=0。【实跑】剩余成本：Finish 两遍归并 1.27 s、Row 0.94 s。
- 发现：私有 Dataset Binding 钉住 `config/catalog.yaml` 摘要，(b) 的 avg 批准使其失效 → 已恢复文件（sha256 ac2dc5cf…，与 02618c1 一致），
  avg 留在校验器能力并有 `internal/catalog` 测试。**Phase 3 前作者需重签绑定**（台账 P8-D1-BINDING）。【读代码推出 + 实跑预检】
- 试点复测已完成并登记（`pilot-evidence-v1.json`：subphase-pilot-08 result-heavy ×3、subphase-pilot-10 analytics ×1，均 bfb6a5b；06/07 标 superseded_by）：
  S6 100k×16 novel 17.40 → 3.46 s，100k×4 4.23 → 1.00 s，S1/SF10 820 → 297 ms；S2/SF10 不变 3.3 s（分组/连接路径）→ 第五刀（1f9b5d1，分组路径并行准备 + 组承诺并发）
  在 S2 形状剖析子测试上 2.37 → 1.49 s，尚未在试点上复核。首次链 pilot-09 因我在运行中快进 wt-pilot 而失败（台账 P8-D1-PILOT-09-FAIL）。
- 磁盘：清理后 71 GB 可用（删了本会话的 deployment/rq5/旧 gateway 镜像与未用卷；作者其他项目的镜像未动）。两次试点预计占 ~30 GB。

## 接着做这个
0a′.（已完成，08:00 CST）链 #8c 全绿：证据 20611c2，终检 rc=0——v1.12 树四道门禁全部收口；接下来 slice b（阶梯 adapter/校验器休眠切片，设计见 docs/p8）。
0a. （已完成，05:20 CST）契约 v1.12 闭环：阶梯 Product 对+预算 profile+AMENDMENT-v1.12+激活固定点（pE-v112-fixedpoint-10 live 全绿，离线 c126487），
   全量 evaluation+vet -tags 绿。接下来：DB 套件+链 #8 → (c2) adapter/校验器休眠切片 → workload 声明激活（周边重钉见 docs/p8 的 v1.12 清单）。
   注意：发射 live 类脚本前脚本自会 pull；不要在其运行中向分支推送或改 wt/主工作树。
0b. （已完成，03:40 CST）(d1)+(b) 全部收口：链 #7 第三次全绿（5ee3054，前两次失败均为测试层：未申报跳过、tag 门控文件丢导入——教训在 verification-cases 2026-08-31），
   证据提交 1c0fc9c，`make paper-final-check` rc=0；pilot-11 已登记（S2/SF10 3.30→2.40 s），pilot-10 标 superseded。
   接下来：Phase 2 契约 v1.12，从 (c2) 开始（`client_ms` 字段 + refused-footprint ladder，设计在 docs/p8_new_evidence_program.md；
   记住：证据 schema/validator 改动要重钉 example 配置里的 protocol/acceptance/statistics sha，且 `go vet -tags taskgate_integration ./...` 后再发链）。
1. （已做）试点登记；`final_v5_pilot_evidence.py` 登记 pilot-08/09（retain 用 mv），对照 pilot-07 的 S1/S2/S6 中位数，把派生优化的端到端效果写进台账；
   若 S6 novel 从 ~17.7 s 降到 ~4–5 s，则论文 Table IX 与 §限制段的成本句子需在 Phase 3 发表级重测后改数（不用试点数改正文）。
2. 试点期间不在 WSL 跑重活。之后跑 v5 链 #7（`bash ~/stage-e/launch-chain.sh`，internal/ 已改），`commit-evidence.sh` 提交证据，`make paper-final-check`。
3. 然后按 P8 顺序：(c2) 被拒探针时延（adapter 逐步计时）、(c3) 良性负载误拒率、(a) 计数器比较臂（registry/activation）、(d2) ≥3 源连接格、(c1) ProvSQL S1–S6。
4. 上报作者（第 1/3 类，等待期不停工）：Phase 3 需重签 dataset binding（可一并决定演示 Catalog 是否批准 avg）。

## 陷阱（本会话新学）
- `go test -cpuprofile` 在 cwd 留下 `gateway.test`；非交互 ssh umask 0002；见 `docs/agents/verification-cases.md` 2026-08-31。
- 流水线路径的行级失败延后到批提交/Finish 才报（fail-closed 不变）；相关单测用 `derivationError` 助手。
