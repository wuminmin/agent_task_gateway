# P8 三项新证据研究报告（供作者裁决论文纳入；全部数字出自已登记 pilot 证据，publication-ineligible）

状态：2026-09-01。三项研究均已闭合、登记于 `evaluation/final-v5-wsl2/pilot-evidence-v1.json`，
四门禁绿。以下文字按补充材料口径起草；正文措辞（若纳入）另行按页预算裁剪。

## 1. 拒绝足迹阶梯（(c2)，pilot-footprint-07）

12 阶聚合阶梯（行数 10^2–10^5 × 参数列 1/2/4，闭式 result_heavy 关系），冻结语料先验给出每阶
Dependency 足迹 rows×(1+fields) 与有界臂预算 400（=最小阶足迹）。实测：
- 无界臂 12/12 接受，逐阶计费与语料精确一致（至 700,000 依赖事实 @100k×4，client 534 ms）。
- 有界臂恰第 1 阶接受（1R/400D/6O，标量 799819/2），其余 11 阶拒绝且零计费，拒绝码按站点先验可导：
  跨度≤400 走预留估算拒（EXPOSURE_BUDGET_EXHAUSTED，2 阶），跨度>400 走证据不可完整拒
  （EXPOSURE_EVIDENCE_REQUIRED，9 阶）。
- 拒绝时延通道（(c2) 本义）：拒绝 26–81 ms vs 接受 65–534 ms；拒绝时延与足迹弱相关
  （预留期拒 ~28–48 ms 常数级；证据期拒随跨度缓增 28–81 ms），供 §timing-channel 讨论引用。

## 2. 良性负载误拒率（(c3)，pilot-benign-05）

27 条未编辑 LLM 生成语句（agentworkload 全部可降低语句）作为单根轨迹，三层先验分类：
5 条策略层拒绝（预算无关：avg 未批 ×1、LIKE ×2、`>` 超产品运算符面 ×2）、6 条零释放、16 条释放。
保守先验配方预算（R=1907, D=1,450,000, O=54, Q=216）与 ×2/×4 档全部在语料冻结于任何执行之前。实测：
- recipe 档恰 1 次预算误拒（q26，位置 19）：轨迹 novel 依赖并集 1,456,837 超配方 D 仅 0.47%。
- ×2 档即零预算误拒（22 接受/5 策略拒=先验理想）；×4 同。
- 跨句集合去重实证：q35 与 q30 重叠 5 行，novel 释放恰 16=1 行×16 列。
- 附带发现并修复一处真实生产缺陷：HAVING 查询全档 INTERNAL_ERROR（语义列宽度不齐，98d20ea 引入，
  585d5d6 修复+回归测试），修复后三条 HAVING 语句正常执行（0 行 0 计费 O=2）。

## 3. 计数器比较臂（(a)，pilot-counter-05）

冻结 100 步自适应轨迹 × 三顺序（自然/洗牌/新颖优先）× 四预算器（exact 三维床 7/12/18、
行计数 175、查询计数 70、仅释放集 7；均为 70% 床先验配方）。全部 12 格逐步命中冻结先验判定表。头条：
- exact 床顺序鲁棒：任何顺序下 distinct 依赖 ≤12、distinct 释放 ≤7（接受 36–46 步后按维拒绝，不归档）。
- 三种朴素计数器在所有配置放走全量 18 依赖事实（行计数放 55–73 步、查询计数恰 70 步、仅释放集 76–78 步）。
- 资源计数器语义：越界查询被截断（第 58 步 5 行放 2 行）并在结算至满时归档，其后一律 TASK_NOT_ACTIVE——
  没有"拒绝当查"的资源步；一维计数不能替代三维集合记账。

## 纳入建议（作者裁决）

- 正文候选一句话each：((c3)) 保守配方误拒 1/22、×2 归零；((a)) exact 床顺序鲁棒 vs 朴素计数器全泄漏。
- 其余进补充材料三小节（各一表）；三项均标注 pilot/publication-ineligible，发表级复测随 Phase 3。
- 依赖作者的前置：avg 批准（q04 将转为可执行）、binding 重签、发表分母裁决。
