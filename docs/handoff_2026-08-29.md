# Handoff 2026-08-29 15:50 CST（campaign 已封存；v5-outcome 证据链全绿入库；PDF 已出）

## 接着做这个

000. **作者授权（2026-08-30 12:2x CST，原文见台账 P7-LOOP-AUTHOR-2026-08-30）：循环持续到隔离 TKDE 评审通过，不再征询选择。** 当前（13:40 CST）：
     - 第 7–9 轮评审均 Major，但意见集合已从「无证据」转到策略约束力/自足性/子相位/计数器基线/生成查询安全 campaign。text-only 项全部落地，主稿 12 页。
     - 已入稿的封存证据：attack 语料、ProvSQL 配对、RLS 自适应轨迹（含独立 oracle 前缀曲线图）、依赖历史 Scale、campaign RQ5、TPC-H 22 条可降低性（0/22）。
     - 两条改口（台账有根因）：被拒探测容量为 ΣB_Q 而非 B_O；ORDER BY 列必须被选中（降低器 `ORDER_FIELD_NOT_SELECTED`），论文原称的排序泄漏不存在。
     - **进行中：** 子相位 pilot `subphase-pilot-06`（WSL `~/wt-pilot`，分支 `subphase-pilot`=a9a0d93；三 profile 各一部署；日志 `~/stage-e/logs/subphase-pilot-subphase-pilot-06.out`）。绿后：把 run 加进 `evaluation/final-v5-wsl2/pilot-evidence-v1.json`（family 待定 "subphase"），生成器出子相位表（pilot 标签），正文 §8.1 写 execute_and_derive 分解；然后在 tip 重跑 v5 链（链 17b9aa1 已绿但落后 tip 生成器）；再起第 10 轮评审。
     - **仍未做的需新证据项：** M4 计数器基线臂（需新 Catalog profile + registry 格，v1.11 契约下较重）；M5 生成查询安全 campaign + F_D 外部交叉校验（ProvSQL where-provenance）；M6 >2 源联接 E2E 格（已改述贡献 2）。
     - 预检清单见记忆 `pilot-campaign-preflight`（qualification 输入、文件权限、命名已推分支、本机 GOPROXY）。

00. **进行中（2026-08-30 12:00 CST）**：封存 campaign 里发现两组从未入稿的 publication-class 证据（attack A–E 语料 135 样本；ProvSQL 配对 810 样本），
    已接入生成器（`_attack_stats`/`_provsql_stats`，measured path）与正文（主稿 §8.2 + 阈值阶梯表；supplement 新增 Attack Corpus / ProvSQL-Paired /
    Gateway Phase Breakdown 三节，证据清单表移入 Number-to-Evidence 节）。主稿剪裁回 12 页（wt-review 构建实测，无 undefined/overfull）。
    **v5 链已于 11:54 CST 在 tip 9b4c87a 发射**（`~/stage-e/logs/post-fix-chain.out`，pid 356037）；之后 tex 又有 5ca73c6/5c392a3 两个 supplement 提交（非 measured path，
    不影响链）。**绿后：** WSL 主工作树提交 DSN 记录 + v5-outcome 四文件 + evidence.tex，`git pull --rebase` 到 origin，推送；NAS 拉取；
    `make paper-final-check`；PDF 到 `tmp/reports/`；台账 P7-LOOP-SEALED-ATTACK-PROVSQL 行补链结果。独立审稿第 7 轮已起（`tmp/reports/reviews/round-7/`）。
    **构建注意：** wt-review 上 `git checkout -- paper/tkde` 会把 evidence.tex 退回旧提交版本，之后必须先 `make paper-evidence` 再 latexmk，否则新宏 undefined。
    **仍未入稿的封存实验：** rls 54、artifact 540、rq5 24 样本（experiment_id 计数，WSL 实查）——是否报告待评估。

0. **审稿循环已停（2026-08-30，第 6 轮后）**：六轮独立评审全部 Major revision；每轮 text-only 意见已落地（分支 `tkde-review-revisions`，
   评审/改动记录在 `tmp/reports/reviews/round-{1..6}/`，裁决项在 `tmp/reports/reviews/open-items.md`）。三项需新证据的意见六轮不变：
   自适应/对抗 agent 工作负载 vs 计数器预算；execute_and_derive 分解 + ProvSQL/计数器消融基线；独立 oracle 规模化并覆盖多路连接。
   **不做实验 verdict 停在 Major**——等作者裁决做哪些，然后按 CLAUDE.md 类别 1 起实验。
1. **分支链已入库、final-check 已过（2026-08-30 11:17 CST）**：链在 tip 464cb8a 全绿（DSN 记录 accepted 4353/0/9、门禁 31 ok、回执 exit 0、schema 3），
   证据提交 2970b07；三方（origin/NAS/WSL）同步到 `840591a`，`make paper-final-check` rc=0，主稿 12 页，PDF 在 `tmp/reports/{main,supplement}-840591a.pdf`（NAS 与 WSL）。
   规则不变：**链运行期间不要在 WSL 主工作树 Sync/pull**（VS Code Sync 会跑 `git pull --tags`，08-30 09:08 曾把 HEAD 移走导致审计失败）。
2. 待作者：合回 `tkde-artifact-rerun`（分支 `tkde-review-revisions` 领先 main dd64038 共 51 提交(git rev-list --count, 08-30 11:25 CST)，含证据与 tex 改动；作者定或授权我做 merge），然后 P7.6 重算审阅清单 SHA。
3. 作者裁决项（open-items）：三项新证据；ORDER BY 字段计入 Dependency（语义改动，需重跑 RQ1–3 与 S3/S6）；工件 DOI/URL 占位；
   匿名署名与 SessionBound 状态句；作者简介（计入 12 页）；§3–4 围绕示例重组（结构性）。
4. 归档 188G 旧 pilot 目录到 NAS（等局域网）；P7.7 cover letter 草稿、P7.8 复现包盘点在 `tmp/reports/`。

## 当前状态（全实测，2026-08-30 11:20 CST）

- git：分支 `tkde-review-revisions` origin=NAS=WSL=`840591a`（含 464cb8a 链证据提交 2970b07 与第 6 轮 tex 改动）；main `tkde-artifact-rerun` 仍在 dd64038。以下为 08-29 主线记录：origin=NAS=WSL=`4a24c2f`（证据入库提交）。今日证据链提交：`74c0e83`(runner 挂 raw) → `8195d02`(linker 输入) → `6aef531`(scale 道可编译 +
  8195d02 记录) → `2016208`(crash-window 步加 tag) → `57aa2d0`(去掉被删测试的 V5 钉) → `aba9d08`(活体 summary 断言跟上 08-16 路由) → `84e0358`(fail() 落容器日志) → `b07841f`(OA 409 带正文) → `706564e`(网关记拒因) → `b2695f7`(门禁 hermetic) → `24173b5`(receipt v10 + cleanup 顺序) → `3683437`(回执 skip 标记三处同改) → `4a24c2f`(证据入库: DSN 记录 + v5-outcome schema 3 + evidence.tex)。
  NAS 有未提交的台账行与本 handoff，等 WSL 证据提交入库后再提交（docs 不在 measured paths 内，不影响 v5 记录）。
- 第九轮链 07:00–07:42Z 全绿：门禁 31 ok、回执 exit 0、schema 3、final 生成通过；`make paper-final-check` 通过；paper 单测 13/13。
- 前八轮失败均已定因并修（第八轮门禁本身已绿）：V5 钉（57aa2d0）、summary 断言（aba9d08）、无容器日志/不透明 409（84e0358/b07841f/706564e）、`.env` 泄入门禁（b2695f7）。
- **formal-v111-publication-03 封存**：campaign-evidence sha `45e5eacd…`，9393 样本全 pass，172 格；`publication-evidence-v1.json` 绑定。
- paper 单测 `test_generate_evidence.py` 13/13 绿（4a24c2f 后）；Makefile 仍无目标跑它——可考虑加入 `make test` 类目标。

## 教训（本会话新增）

- 删任何源文件前 grep `paper/tkde/generate_evidence.py` 的 `V5_SOURCE_PATHS`（32 项绑定）。
- 门禁活体段断言与 `config/catalog.yaml` 路由是发射/校验的一对；改路由必须 grep `scripts/integration-test.sh`。
- 门禁必须 hermetic：compose 会读工作树 `.env` 补未设变量；任何「只在执行机上失败」先查 `.env` 变量名（不看值）。
- 包装脚本的前置谓词要抄脚本的真实检查（`record-compose-e2e.sh:26` 只查 measured paths；审计 `scope.go:285` 只查记录 commit==HEAD），
  不抄 README 散文。
- 后台启动一律走 `~/stage-e/launch-*.sh` 文件（inline nohup/setsid 会被权限策略拦）。
