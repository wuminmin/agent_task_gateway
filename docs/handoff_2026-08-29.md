# Handoff 2026-08-29 15:50 CST（campaign 已封存；v5-outcome 证据链全绿入库；PDF 已出）

## 接着做这个

0. **审稿修订分支（2026-08-30）**：模拟 TKDE 审稿 `tmp/reports/tkde-review-simulated.md`（Major revision，M1–M8）；修订工作在分支
   `tkde-review-revisions`（2ebee4f，已推送）：replay 措辞收窄、SessionBound 增量段、O2 去重、supplement TLC 配置表、
   分阶段延迟分解（`_phase_stats`）与同 root 并发轮次（`_concurrency_stats`）的 generator 宏与表，单测 7/7 在 NAS 过。
   **该分支未 final-check 干净**：`generated/evidence.tex` 要在 WSL 用真实样本 `make paper-evidence` 重生成后才含新宏。
   WSL 回来后：checkout 分支 → `make paper-evidence`（若报缺阶段键/并发格，改 generator 不改样本）→ `make paper-tkde` 核编译与页数
   → 提交 evidence.tex → `make paper-final-check` → 合回 `tkde-artifact-rerun`。待作者裁决三项：同机成本参照实验、
   Spider2.0/BIRD 可接纳率研究、对抗片段驱动。
1. **作者看 PDF**：WSL `tmp/reports/main-4a24c2f.pdf`（13 页）/`supplement-4a24c2f.pdf`——新增 campaign 段落、`tab:current-evidence` 的
   Campaign 行、Reproducibility Boundary 改写、supplement 的 baseline 表（作者定「2 先加，我看看效果」）。**P7.5 目标 12 页，现 13 页**，
   删什么由作者定；决定后改 tex → `make paper-evidence` 无需（evidence.tex 已入库）→ `make paper-final-check` 复跑。
2. P7.6：作者逐字节审批前，重算 `docs/final_v5_author_review_manifest.md` 的 SHA-256（main.tex/supplement.tex/protocol/workloads/
   catalog/claim matrix）——等正文定稿后再算，避免反复；Claude 只算不确认。
3. `tmp/reports/decision-38-draft.md` 待作者确认；`tmp/reports/p78-repro-package-inventory.md` 列了 P7.8 的三个作者裁决点
   （DataPort vs Zenodo、traces 粒度、留存字节 sha 清单）。
4. 归档：188G 旧 pilot 目录等作者回到 NAS 局域网后经内网搬 NAS（先测吞吐）。WSL 根盘约 144G 可用。
5. P7.2/P7.3/P7.4/P7.7/P7.9 按清单推进；P7.5 摘要已核（179 词、无引用无公式）。

## 当前状态（全实测，2026-08-29 15:50 CST）

- git：origin=NAS=WSL=`4a24c2f`（证据入库提交）。今日证据链提交：`74c0e83`(runner 挂 raw) → `8195d02`(linker 输入) → `6aef531`(scale 道可编译 +
  8195d02 记录) → `2016208`(crash-window 步加 tag) → `57aa2d0`(去掉被删测试的 V5 钉) → `aba9d08`(活体 summary 断言跟上 08-16 路由) → `84e0358`(fail() 落容器日志) → `b07841f`(OA 409 带正文) → `706564e`(网关记拒因) → `b2695f7`(门禁 hermetic) → `24173b5`(receipt v10 + cleanup 顺序) → `3683437`(回执 skip 标记三处同改) → `4a24c2f`(证据入库: DSN 记录 + v5-outcome schema 3 + evidence.tex)。
  NAS 有未提交的台账行与本 handoff，等 WSL 证据提交入库后再提交（docs 不在 measured paths 内，不影响 v5 记录）。
- 第九轮链 07:00–07:42Z 全绿：门禁 31 ok、回执 exit 0、schema 3、final 生成通过；`make paper-final-check` 通过；paper 单测 13/13。
- 前八轮失败均已定因并修（第八轮门禁本身已绿）：V5 钉（57aa2d0）、summary 断言（aba9d08）、无容器日志/不透明 409（84e0358/b07841f/706564e）、`.env` 泄入门禁（b2695f7）。
- **formal-v111-publication-03 封存**：campaign-evidence sha `45e5eacd…`，9393 样本全 pass，172 格；`publication-evidence-v1.json` 绑定。
- paper 单测 `test_generate_evidence.py` 13/13 绿（4a24c2f 后）；Makefile 仍无目标跑它——可考虑加入 `make test` 类目标。

## 教训（本会话新增）

- 删任何源文件前 grep `paper/tkde/generate_evidence.py` 的 `V5_SOURCE_PATHS`（32 项绑定）。
- 门禁活体段断言与 `config/catalog.yaml` 路由是发射/校验的一对；改路由必须 grep `scripts/integration-test.sh`。
- 门禁必须 hermetic：compose 会读工作树 `.env` 补未设变量；任何「只在执行机上失败」先查 `.env` 变量名（不看值）。
- 包装脚本的前置谓词要抄脚本的真实检查（`record-compose-e2e.sh:26` 只查 measured paths；审计 `scope.go:285` 只查记录 commit==HEAD），
  不抄 README 散文。
- 后台启动一律走 `~/stage-e/launch-*.sh` 文件（inline nohup/setsid 会被权限策略拦）。
