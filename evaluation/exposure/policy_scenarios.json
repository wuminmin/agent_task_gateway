{
  "schema_version": 1,
  "fixture": "evaluation/exposure/corpus.json",
  "utility_metric": "fraction of predeclared workflow goals admitted",
  "budget_percentages": [25, 50, 75, 100],
  "scenarios": [
    {
      "id": "finance_summary",
      "label": "Finance reimbursement summary",
      "policy_context": "A finance approver permits totals and departmental trends but no employee-level lookup.",
      "sensitive_fields": [],
      "steps": [
        {"id": "claim_count", "operation": "global_count", "aggregate": true},
        {"id": "amount_total", "operation": "global_sum", "aggregate": true},
        {"id": "department_breakdown", "operation": "group_sum_count", "aggregate": true}
      ]
    },
    {
      "id": "case_review",
      "label": "Scoped reimbursement case review",
      "policy_context": "An auditor pages through a bounded case list before requesting the complete department-and-amount view.",
      "sensitive_fields": ["expense.amount"],
      "steps": [
        {"id": "first_page", "operation": "page_first_four", "aggregate": false},
        {"id": "overlap_page", "operation": "page_middle_five", "aggregate": false},
        {"id": "complete_review", "operation": "projection_pair", "aggregate": false}
      ]
    },
    {
      "id": "delegated_escalation",
      "label": "Delegated exception escalation",
      "policy_context": "A root reviewer delegates a sales drill-down and a manager lookup after an aggregate exception signal.",
      "sensitive_fields": ["department.manager", "expense.amount"],
      "steps": [
        {"id": "exception_signal", "operation": "group_hidden_key_sum", "aggregate": true},
        {"id": "sales_drilldown", "operation": "selection_sales_amount", "aggregate": false},
        {"id": "manager_lookup", "operation": "department_join", "aggregate": false}
      ]
    }
  ]
}
