{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "taskgate-final-v5-adapter-operation-v1",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema_version", "campaign_class", "campaign_id", "deployment_id", "experiment_id", "cell_id", "sample_id", "iteration", "process_replicate", "order_position", "random_seed", "warmup", "fresh_root_required", "root_group_id", "workload_id", "scale", "mode"],
  "properties": {
    "schema_version": {"const": 1},
    "campaign_class": {"enum": ["pilot", "publication"]},
    "campaign_id": {"type": "string", "minLength": 1},
    "deployment_id": {"type": "string", "pattern": "^deployment-[0-9]{2}$"},
    "experiment_id": {"enum": ["baseline", "scale", "artifact", "rls", "attack", "provsql", "compiler", "concurrency", "rq5"]},
    "cell_id": {"type": "string", "minLength": 1},
    "sample_id": {"type": "string", "minLength": 1},
    "iteration": {"type": "integer", "minimum": 1},
    "process_replicate": {"type": "integer", "minimum": 1},
    "order_position": {"type": "integer", "minimum": 1},
    "random_seed": {"type": "integer"},
    "warmup": {"type": "boolean"},
    "fresh_root_required": {"type": "boolean"},
    "root_group_id": {"type": "string", "minLength": 1},
    "workload_id": {"type": "string", "minLength": 1},
    "scale": {"type": "string", "minLength": 1},
    "mode": {"type": "string", "minLength": 1}
  }
}
