# Final V5 evidence destination

This directory is intentionally empty until an author completes and validates a frozen publication campaign. Pilot output and mutable raw run directories must not be copied here. A future sealed pack must include its manifest, raw-file digests, environment/deployment manifests, validator receipt, and frozen commit.
