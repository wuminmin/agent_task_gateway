[
  {
    "type": "function",
    "function": {
      "name": "execute_plan",
      "description": "Execute one authorized declarative query plan. The study harness supplies task and request identifiers.",
      "parameters": {
        "type": "object",
        "additionalProperties": false,
        "required": ["plan"],
        "properties": {
          "plan": {
            "type": "object",
            "description": "TaskGate QueryPlan with one of product or from, plus columns and optional aggregates, filters, group_by, order_by, limit, and offset."
          },
          "output_format": {"type": "string", "enum": ["json", "table"]}
        }
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "get_budget",
      "description": "Read the active study policy's budget limits, usage, and remaining capacity without converting between policy units.",
      "parameters": {"type": "object", "additionalProperties": false, "properties": {}}
    }
  }
]
