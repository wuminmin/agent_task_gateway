# Raw results

Each successful run creates a new directory containing `run.json`,
`samples.jsonl`, `samples.csv`, and `cells.jsonl`. These files are ignored by
default because performance data must be produced on the declared evaluation
environment. Archive the exact run directories used for a paper separately;
never replace them with hand-entered values.
