\IfFileExists{IEEEtran.cls}{%
  \documentclass[10pt,journal,compsoc]{IEEEtran}
}{%
  \documentclass[10pt,twocolumn]{article}
}

\usepackage[T1]{fontenc}
\usepackage{amsmath,amssymb,amsthm}
\usepackage{array}
\usepackage{booktabs}
\usepackage{cite}
\usepackage{graphicx}
\usepackage{pgfplots}
\usepackage{tikz}
\usetikzlibrary{arrows.meta,fit,positioning,shapes.geometric,shapes.symbols}
\pgfplotsset{compat=1.18}
\usepackage[expansion=false]{microtype}
\usepackage{xcolor}
\usepackage{xspace}
\usepackage{url}
\usepackage[hidelinks]{hyperref}

\newcolumntype{L}[1]{>{\raggedright\arraybackslash}p{#1}}
\providecommand{\IEEEPARstart}[2]{#1#2}
\makeatletter
\@ifundefined{IEEEkeywords}{%
  \newenvironment{IEEEkeywords}{\par\small\noindent\textbf{Index Terms---}}{\par}
}{}
\makeatother

\newcommand{\system}{TaskGate\xspace}
\newcommand{\code}[1]{\texttt{\detokenize{#1}}}
\newcommand{\release}{\mathsf{release}}
\newcommand{\facts}{\mathcal{F}}
\newcommand{\accounted}{\mathcal{K}}
\newcommand{\effect}{\mathcal{E}}
\newcommand{\rieffect}{\mathcal{E}^{ri}}
\newcommand{\lbag}{\{\!\{}
\newcommand{\rbag}{\}\!\}}
\newcommand{\llbracket}{[\![}
\newcommand{\rrbracket}{]\!]}
\newtheorem{definition}{Definition}
\newtheorem{property}{Property}
\newtheorem{theorem}{Theorem}

\input{generated/evidence.tex}

\title{TaskGate: Task-Scoped Query-Outcome and Exposure Accounting}

\author{Anonymous Author(s)%
\thanks{The artifact includes a controlled local PostgreSQL performance
campaign and a three-scale TPC-H-derived Join--Group campaign. Results remain
single-host evidence; the latter is not an official TPC-H run and neither
campaign claims production generality.}}
\date{}

\begin{document}
\maketitle

\begin{abstract}
Adaptive clients can accumulate substantial disclosure through individually
authorized rewrites, pages, retries, and delegated queries. We present
TaskGate, which meters an approved root task with three sets of typed FactIDs:
values released to the client, conservative positive-output dependencies, and
successful query outcomes.  An outcome fact binds the normalized query
proposition to a digest of its released result, so distinct predicates that
both return an empty relation or zero do not collapse to one free replay.
Before any result leaves the gateway, an authorized visible query and its
provenance companion run in one PostgreSQL snapshot; TaskGate buffers the
answer, derives all three effects, and atomically inserts only facts novel to the
root-family ledger. The central safety property is that every committed ledger
equals the union of settled novel effects and never exceeds any signed
limit, so replay and delegation cannot mint exposure capacity.

We define compositional semantics for a positive relational fragment with
joins, union-distinct, grouping, and four aggregates, and check the settlement
protocol with a finite-state model. Independent-oracle, rewrite, concurrency,
and PostgreSQL tests validate the modeled guarantees. A fixture-based policy
calibration shows how three operational policies induce release/dependency
budget--utility frontiers under a separate outcome ceiling. End-to-end
experiments reach \RQFourScaleMaxFacts{} dependency facts, but novel/replay
latencies of \RQFourScaleHighNovelS{}/\RQFourScaleHighReplayS{}~s and
\RQFourScaleGatewayGiB{}~GiB gateway memory expose a severe boundary for the
current exact materialization design.
\end{abstract}

\begin{IEEEkeywords}
data exposure, query auditing, provenance, adaptive workloads, cumulative
budget, positive-output dependency, query-outcome accounting
\end{IEEEkeywords}

\section{Introduction}
\label{sec:introduction}

\IEEEPARstart{D}{atabase} authorization answers whether a principal may execute
a particular operation. An adaptive client creates a second problem: during a
single human-approved task it can adaptively issue many individually legal
queries. It can split a projection, overlap pages, retry with a new request
identifier, ask an equivalent query, or delegate a narrower subtask. Per-query
authorization and row limits do not state how much distinct data the resulting
root ledger has already accounted. This is not a model of the agent's knowledge.

Counting queries is insensitive to answer size. Counting returned rows makes a
one-row aggregate appear cheap even when it depends on millions of source rows.
Counting bytes or cells varies with encoding and query shape. Monetary query
pricing has developed formal anti-arbitrage objectives
\cite{koutris2012query,deep2017qirana,peng2025aria}, while provenance explains
where results came from \cite{green2007provenance,amsterdamer2011aggregate}.
Our question is narrower and operational: how can a human authorization create
a stateful, explicit-disclosure budget that remains conserved while adaptive
clients query and delegate? The mechanism neither inspects an LLM nor depends
on an agent-specific planning algorithm; agents are one important client class.

\system treats the root task as the budget subject. A result has three effects.
\emph{Release exposure} identifies values actually delivered.
\emph{Positive-output dependency} is a contract-defined conservative footprint
of base facts participating in the successful derivation of delivered output.
It is neither minimal causal provenance nor the complete physical read set.
This second dimension prevents a count from being accounted as one input fact
merely because it returns one cell. Both effects are sets, so a fact already
recorded in the root-family ledger has zero incremental charge.  The third,
\emph{query outcome}, binds the normalized predicate and relational context to
the released-result digest and visible cardinality. Thus ``no row satisfies
$x>100$'' and ``no row satisfies $x>200$'' consume two outcome facts even when
their release and dependency sets coincide; an exact replay remains free.

The system follows a withhold-before-settlement discipline:

\begin{center}
\small
\code{reserve -> execute/buffer -> derive -> settle -> release}.
\end{center}

The visible query and its provenance companion observe one PostgreSQL snapshot.
The gateway releases neither until a control-database transaction has inserted
novel facts, checked all three root limits, settled resource use, stored the encrypted
result, and persisted audit and receipt evidence. A failed limit check rolls the
transaction back while the business execution remains separately measurable.
Figure~\ref{fig:architecture-settlement} shows the trust boundary and the two
transactional phases.

\begin{figure*}[t]
\centering
\resizebox{0.98\textwidth}{!}{%
\begin{tikzpicture}[
  font=\scriptsize,
  box/.style={draw, rounded corners=1.5pt, align=center, minimum height=7mm,
    inner xsep=4pt, fill=blue!5},
  store/.style={draw, cylinder, shape border rotate=90, aspect=.25,
    align=center, minimum height=9mm, minimum width=16mm, fill=green!7},
  txnbox/.style={draw, rounded corners=1.5pt, align=center, minimum width=19mm,
    minimum height=7mm, inner xsep=3pt, fill=blue!5},
  decision/.style={draw, diamond, aspect=2.0, align=center, inner sep=1.5pt,
    fill=yellow!12},
  arrow/.style={-{Latex[length=2mm]}, semithick},
  exchange/.style={{Latex[length=2mm]}-{Latex[length=2mm]}, semithick},
  return/.style={-{Latex[length=2mm]}, semithick, dashed}
]
% (a) Architecture
\node[font=\small\bfseries] at (3.25,4.0) {(a) Architecture and trust boundary};
\node[box, fill=red!5] (client) at (0.55,2.55) {adaptive client\\root + delegates\\plan / result};
\node[box] (approval) at (0.55,.55) {human approval\\signed grant};
\node[box, minimum width=24mm, minimum height=12mm] (gateway) at (3.35,2.55)
  {TaskGate gateway\\authorize $\rightarrow$ compile\\buffer $\rightarrow$ derive};
\node[store] (business) at (6.25,2.55) {Business PG\\query pair\\one snapshot};
\node[store] (control) at (4.95,.55) {Control PG\\budgets + ledgers\\receipt / reject};
\draw[exchange] (client) -- (gateway);
\draw[arrow] (approval) -- (gateway);
\draw[exchange] (gateway) -- (business);
\draw[exchange] (gateway) -- (control);
\node[draw, dashed, rounded corners, inner sep=3mm,
  fit=(gateway)(business)(control), label={[font=\scriptsize]below:trusted enforcement boundary}] {};

% divider
\draw[gray!55] (7.65,.05) -- (7.65,3.85);

% (b) Settlement sequence
\node[font=\small\bfseries] at (11.95,4.0) {(b) Withhold-before-settlement transaction};
\node[txnbox] (reserve) at (8.7,2.65) {reserve};
\node[txnbox] (execute) at (10.65,2.65) {Business tx\\execute + buffer};
\node[txnbox] (derive) at (12.75,2.65) {derive\\$(\facts_r,\facts_i,\facts_o)$};
\node[txnbox] (lock) at (14.85,2.65) {Control tx\\lock root};
\node[decision] (fitbudget) at (14.85,1.35) {$K\cup\Delta$\\within all limits?};
\node[txnbox, fill=green!10] (commit) at (12.55,.35) {commit facts, result,\\audit + receipt};
\node[txnbox, fill=green!18] (releasebox) at (9.65,.35) {release buffer};
\node[txnbox, fill=red!8] (rollback) at (16.55,.35) {rollback facts/result\\withhold buffer};
\draw[arrow] (reserve) -- (execute);
\draw[arrow] (execute) -- (derive);
\draw[arrow] (derive) -- (lock);
\draw[arrow] (lock) -- node[right]{insert novel FactIDs} (fitbudget);
\draw[arrow] (fitbudget) -- node[above, sloped]{yes} (commit);
\draw[arrow] (commit) -- (releasebox);
\draw[arrow] (fitbudget) -- node[above, sloped]{no} (rollback);
\end{tikzpicture}
}
\caption{TaskGate architecture and settlement. Business PostgreSQL produces
the visible answer and positive evidence in one read-only snapshot. Control
PostgreSQL serializes the root family and commits the exact novel FactSet before
the gateway releases the buffered answer.}
\label{fig:architecture-settlement}
\end{figure*}

TaskGate is a direct successor to SessionBound
\cite{wu2026sessionbound}. SessionBound already turns task approval into a
short-lived database session with signed tokens, SQL/view/scope boundaries,
query and disclosure budgets, and allow/deny receipts. We treat that substrate
as prior work and do not claim those mechanisms here. Relative to it, this
paper makes five implementation-backed contributions:

\begin{enumerate}
\item A versioned, typed FactID and three-set semantics separating delivered
release facts, positive-output dependencies, and normalized query outcomes in
a restricted relational algebra.
\item A same-snapshot provenance companion for one-product plans and bounded
two-scan inner equijoins/union-distinct, including grouping and four aggregates.
\item Exact novel-set settlement: atomically charge
$E_q\setminus\accounted_T$, withhold on any limit, and make semantic replay
zero-charge rather than merely incrementing a per-query counter.
\item Signed, monotonic root-family delegation whose descendants share one
three-dimensional ledger, preventing task-tree fan-out from multiplying exposure capacity.
\item New formal properties and evidence for these mechanisms: three-budget
safety, exact/non-overlapping novel charge, no pre-settlement delivery, and
task-family non-amplification; algebraic/rewrite tests, a finite TLA+ ledger
model, and full-path PostgreSQL experiments through one million dependency
facts.
\end{enumerate}

The implementation does \emph{not} claim arbitrary SQL equivalence, differential
privacy, inference control, principal-wide accounting, or denial-oblivious
auditing. The online multi-input grammar is intentionally bounded: two scan
leaves, an inner conjunctive equijoin or same-product union-distinct, and no
nested set/join tree or multi-input pagination. A newly approved root creates a
distinct ledger. Outcome accounting bounds the number of distinct successful
propositions admitted by this grammar; it is not an information-theoretic
bound on the bits revealed by one proposition. These boundaries are part of the result, rather than hidden
behind a broad ``SQL support'' label.

The commercial fit is a governed analytics or operations copilot operating
inside one approved case: finance agents testing reimbursement/credit
thresholds, fraud or compliance investigators refining ``no matching case''
queries, customer-support agents searching entitlement conditions, and
delegated analysts splitting one investigation. Release limits cap repeated
value extraction, dependency limits expose large-source aggregates, and the
outcome limit makes adaptive yes/no or empty-result probing consume explicit
case capacity. Organizations that need population-level privacy across many
approved roots still require differential privacy or a tenant-wide policy;
TaskGate supplies auditable workflow conservation, not that replacement.

\section{Context and Related Work}
\label{sec:related}

\subsection{Closest Systems and Incremental Novelty}

SessionBound is the immediate architectural predecessor
\cite{wu2026sessionbound}. It already contributes the control-plane path from
business task templates and approval to signed task tokens, and a PostgreSQL
runtime enforcing safe views, row scope, denied fields, operation/query and
disclosure budgets, plus receipts. Its disclosure units may be rows, bytes, or
unique entity identifiers. TaskGate therefore does not present task approval,
SQL mediation, cumulative disclosure budgets, or receipts as new. It changes
the accounted object and settlement semantics: values and successful
derivations become stable FactID sets; release, positive-output dependency, and
normalized query outcomes have separate ledgers; the answer and evidence are obtained in one snapshot;
only exact set difference is charged; and all delegated descendants settle one
root-family ledger.

GAAP is the closest system at the agent data-flow layer
\cite{stanley2026gaap}. It places private data and permissions outside the
untrusted model, analyzes agent-generated code with information-flow control,
uses trusted tool annotations, and records disclosures to external entities in
a persistent log. That log propagates taint across calls and tasks, addressing
indirect cross-service disclosure that TaskGate explicitly does not solve.
Conversely, GAAP does not define a SQL value-level FactID algebra, distinguish
delivered database values from positive-output dependencies, co-execute a
PostgreSQL provenance companion, or settle an exact per-root novel FactSet.
Table~\ref{tab:closest-systems} states the boundary without treating different
threat models as an overall ranking.

\begin{table*}[t]
\centering
\caption{Capability and guarantee comparison. \checkmark{} is an explicit
guarantee in the cited design; ``hist.'' denotes history-aware accounting with
a different unit or scope; -- denotes not claimed. The table compares scopes,
not overall system quality.}
\label{tab:closest-systems}
\scriptsize
\setlength{\tabcolsep}{3.2pt}
\resizebox{\textwidth}{!}{%
\begin{tabular}{lccccccc}
\toprule
System family & Approved task/root & Typed DB FactIDs & Release/dependency split & Same-snapshot evidence & Exact novel-set pre-release & Shared-root delegation & Cross-tool IFC \\
\midrule
TaskGate & \checkmark & \checkmark & \checkmark & \checkmark & \checkmark & \checkmark & -- \\
SessionBound~\cite{wu2026sessionbound} & \checkmark & -- & -- & -- & hist. units & -- & -- \\
GAAP~\cite{stanley2026gaap} & persistent log & -- & -- & -- & hist. disclosure & -- & \checkmark \\
UCON / history-based AC~\cite{park2004ucon,abadi2003history} & ongoing use & -- & -- & -- & hist. actions & policy-specific & -- \\
Query pricing~\cite{koutris2012query,deep2017qirana,peng2025aria} & query/history & -- & -- & -- & hist. price & -- & -- \\
Provenance systems~\cite{arab2018gprom,senellart2018provsql} & query & tuple/annotation & -- & rewrite/native & -- & -- & -- \\
\bottomrule
\end{tabular}}
\end{table*}

\subsection{History-Based Query Auditing and Inference Control}

Database auditing has long treated a query log, rather than an isolated query,
as the security object. Hippocratic-database auditing reconstructs which logged
queries accessed protected data \cite{agrawal2004hippocratic}, while batch SQL
auditing asks whether a sequence of answers determines a forbidden view
\cite{motwani2008auditing}. Simulatable auditing additionally prevents the
accept/deny bit from leaking the protected value \cite{kenthapadi2005simulatable};
inference-control surveys cover accumulated answers and external knowledge
\cite{farkas2002inference}. These results
rule out a broad ``history makes querying safe'' claim. \system is deliberately
narrower: it neither models an adversary's prior knowledge nor protects against
denial leakage or implicit logical inference. It meters a successful empty or
zero answer as one normalized proposition, but does not estimate the inference
content of that proposition. Its history is one signed root task
and all descendants rather than a global user history. Simulatable or
knowledge-aware inference control is complementary and could sit outside the
gateway.

\subsection{Query Pricing and Privacy Accounting}

Query-based pricing derives monetary prices from priced views and studies
arbitrage induced by query determinacy \cite{koutris2012query}. QIRANA extends
scalable, history-aware pricing to a broad SQL class including aggregation
\cite{deep2017qirana}; ARIA uses tuple information gain for linear-time pricing
of SPJA and simple aggregate queries \cite{peng2025aria}. Thus attribute
weighting, history-aware charging, and rewrite resistance are not new by
themselves. \system instead treats price as an admission budget over explicit
contract facts, scopes state to a task family, and withholds delivery when the
atomic settlement fails.

History also matters in differential privacy systems. CacheDP reuses past noisy
answers to reduce privacy spending \cite{mazmudar2022cachedp}, and DProvDB tracks
privacy loss by analyst provenance \cite{zhang2023dprovdb}. These systems reason
about randomized mechanisms and privacy composition
\cite{dwork2014foundations}. \system releases exact answers and therefore offers
no $\epsilon$-DP guarantee; its budget bounds only facts recognized by the
active exposure profile.

\subsection{Provenance Foundations and Systems}

Why- and where-provenance identify contributing data and output locations
\cite{buneman2001why}, while lineage traces warehouse outputs back through
transformations \cite{cui2001lineage}; Cheney et al. survey the resulting
database-provenance design space \cite{cheney2009provenance}. Semiring
provenance provides compositional annotations for positive relational queries
\cite{green2007provenance}, and aggregation requires value-level treatment
beyond tuple annotations \cite{amsterdamer2011aggregate}.

Provenance is also an implemented database capability: GProM rewrites
provenance questions \cite{arab2018gprom}, while ProvSQL adds provenance and
probability to PostgreSQL \cite{senellart2018provsql}. \system claims
neither a new provenance algebra nor a general provenance engine. It specializes
provenance to two base set-valued effects---facts explicitly released and a
conservative operator-input footprint for their successful positive
derivations---then requires answer/effect co-generation in
one snapshot and atomic settlement before release.  The online profile includes
bounded inner-equijoin and union-distinct multiplicity; outer joins, negation,
and general recursive SQL remain outside it.

\subsection{Usage Control, Delegation, and Quotas}

Usage control extends authorization across an ongoing use \cite{park2004ucon};
execution-history access control makes past actions part of authorization
\cite{abadi2003history}; and delegation has mature logical foundations
\cite{li2003delegation}. PERMIS combines delegated authorization with
history-sensitive cumulative-resource decisions \cite{chadwick2008permis}.

Consequently, neither token attenuation, delegation depth, consumable authority,
nor quota accounting is claimed independently. \system's narrower systems claim
is that every descendant settles the same semantic disclosure effect against a
single root-task exposure ledger, so delegation cannot mint a fresh disclosure
budget. The prototype uses centralized transactional settlement; distributed
vouchers and reconciliation would require an additional safety argument.

\subsection{Database Policy Enforcement and Agents}

Database-backed policy systems rewrite or validate queries against application
policy \cite{mehta2017qapla,zhang2022blockaid}. Recent work aligns
natural-language interfaces with database policy \cite{liu2026safenlidb},
measures indirect prompt injection against tool-using agents
\cite{debenedetti2024agentdojo}, and applies runtime data-flow control
\cite{summers2026dataflow}.

These systems motivate treating generated SQL and client tools as untrusted,
but pursue complementary targets: per-request policy, prompt-injection
resilience, or information-flow labels.
\system places the model outside the trusted computing base and focuses on
cumulative, explicit database disclosure across retries, pagination, supported
rewrites, and delegation.

\textbf{Novelty boundary.} History, inference control, pricing, provenance,
query mediation, delegation, quotas, task-bound SQL sessions, receipts, and
persistent agent disclosure logs are established by the literature above.
Relative to SessionBound, the claim is precisely FactID three-set semantics,
same-snapshot provenance, exact novel-set settlement, shared root-family
delegation, and the corresponding formal properties and experiments. Relative
to GAAP, the contribution is database-specific semantic accounting rather than
general cross-task IFC. This boundary is narrower than SQL noninterference or
GAAP's confidentiality goal and stronger than stateless authorization for the
modeled positive-disclosure threat.

\section{Exposure Model}
\label{sec:model}

\subsection{Facts and Task State}

The V2 release/dependency algebra treats a FactID as a semantic object and its SHA-256 as only a database
index.  Its semantic environment is
$\Pi=\langle pg,\mathcal T,UTC,\mathcal C,D,h_C\rangle$: a PostgreSQL major
version, admitted type domain, timezone, exact deterministic collation
name/version map, immutable snapshot map, and catalog/lineage digest.  A
judgment is defined only for an \emph{admissible} $\Pi$: source schemas and
entity keys are attested, every collatable field has a deterministic collation,
and volatile expressions are absent.  Let $\nu$ be a catalog-owned source
namespace (not a view alias), $s$ an immutable snapshot, $e$ a canonical stable
entity key, $a$ a role-qualified field ID, $\tau\in\mathcal T$, and
$C_\tau(v)$ its typed canonical value.  Base identities are

\begin{align}
 f_{row}&=\langle\pi,\nu,s,e\rangle,\\
 f_{cell}&=\langle\pi,\nu,s,e,a,\tau,C_\tau(v)\rangle .
 \label{eq:fact}
\end{align}

A derived identity is
$f_d=\langle\pi,\mathcal B,k_o,N_\pi(x),\tau,C_\tau(v),CW(W)\rangle$,
where $\mathcal B=\operatorname{sort}\{(\nu,s)\}$ is the relation's snapshot
bundle, $k_o$ is the canonical output-row entity key, $N_\pi(x)$ erases aliases
and normalizes the supported expression, and
$W:\facts_{base}\rightharpoonup\mathbb N^+$
is a finite-support witness multiset (the empty support is allowed).  The product identity is not a caller-supplied
name: it is exactly the namespace projection of $\mathcal B$.  The witness
commitment $CW(W)$ length-prefixes the lexically sorted
$(\operatorname{FactHash}(f),W(f))$ pairs; the empty multiset has a separate
token.  The durable key is
$\mathrm{SHA256}(\texttt{TASKGATE-FACT-V2\textbackslash0}\|\operatorname{Canon}(f))$.
Control PostgreSQL stores $\operatorname{Canon}(f)$ and compares it on every hash conflict;
a same-hash, different-payload row aborts the transaction. V1 and V2 tagged
payloads are disjoint. V3 preserves these V2 identities and adds an outcome
kind under a disjoint hash domain.

Let $N(q)$ be the accepted query normal form, $h_N$ its SHA-256, $m$ the
visible row count, and
$h_R=H(m,\operatorname{sort}\{\operatorname{Canon}(f):f\in\facts_r\})$.
V3 creates exactly one successful-outcome identity
\begin{equation}
 f_o=\langle\pi_3,\operatorname{ver}(N),h_N,h_R,m\rangle,
 \qquad
 \operatorname{FactHash}(f_o)=
 H(\texttt{TASKGATE-FACT-V3\textbackslash0}\|\operatorname{Canon}(f_o)).
 \label{eq:outcome-fact}
\end{equation}
The normal form contains the Catalog namespace, snapshot/lineage binding,
typed predicates, grouping, ordering, pagination, null/bag semantics,
collations, timezone, and exact numeric mode. It erases only the explicitly
supported aliases and reorderings. Therefore the proposition is server-bound,
not caller-named; the result commitment is explicit even for $m=0$ or a
one-row scalar zero.

\begin{theorem}[Fact identity well-definedness]
For every value in the V2 supported PostgreSQL type domain, canonical encoding
is deterministic and injective over the typed semantic payload (that is, over
PostgreSQL semantic-value equivalence, not raw float NaN bit patterns).
\end{theorem}
\begin{proof}
Each scalar encoder is a canonical representative of its typed equivalence
class: bounded integers use signed decimal, exact numerics reduced rationals,
floats fixed-width hexadecimal with canonical NaN and zero, temporal values
PostgreSQL microseconds (UTC for \code{timestamptz}), and NULL a disjoint token.
Every tuple component is type tagged and length delimited; every list is count
prefixed and sorted where its mathematical carrier is a set.  Decoding first
recovers the unique tag and component boundaries, then the unique scalar
representatives.  Therefore equal encodings imply equal typed payloads, and
determinism follows from the unique representative and ordering functions.
\end{proof}

\begin{definition}[Exposure effect]
For query $q$ over snapshot $D_s$, the V3 effect
$\effect(q,D_s)=\langle \facts_r,\facts_i,\facts_o\rangle$ contains release facts
$\facts_r$ and positive-output dependency facts $\facts_i$ under a versioned
profile $\pi$.  The latter are the conservative base facts selected by the
inductive rules below for successfully derived delivered rows and cells; they
are not claimed to be minimal causal provenance or all physical reads.
$\facts_o=\{f_o\}$ contains the successful proposition/result identity from
Eq.~\eqref{eq:outcome-fact}.
\end{definition}

Every root task $T$ stores previously accounted sets
$\accounted_T=\langle\accounted_T^r,\accounted_T^i,\accounted_T^o\rangle$ and limits
$B_T=\langle B_T^r,B_T^i,B_T^o\rangle$. Settlement computes

\begin{align}
 \Delta_T^r(q) &= \facts_r(q,D_s)\setminus\accounted_T^r,\\
 \Delta_T^i(q) &= \facts_i(q,D_s)\setminus\accounted_T^i,\\
 \Delta_T^o(q) &= \facts_o(q,D_s)\setminus\accounted_T^o.
 \label{eq:delta}
\end{align}

V3 measures cardinality independently in each dimension. A query may settle
only if
$|\accounted_T^r|+|\Delta_T^r|\leq B_T^r$ and
$|\accounted_T^i|+|\Delta_T^i|\leq B_T^i$ and
$|\accounted_T^o|+|\Delta_T^o|\leq B_T^o$.

The word ``accounted'' is intentional. Release facts describe delivered values,
but dependency FactIDs remain internal to the gateway; the agent receives
only result data, counts, and receipt digests. Hence $\accounted_T^i$ is not a
set of facts known by the agent, and no component is a complete epistemic
or privacy-loss state.  The wire and storage label \code{influence} is retained
for compatibility; under the V2/V3 profiles it denotes positive-output dependency,
not causal influence.

\begin{theorem}[Outcome separation and replay]
For successful queries over the same snapshot, unequal normal-form digests
produce unequal outcome FactIDs even when their released results are equal.
Equal normal forms and equal released FactSets/cardinalities produce the same
outcome FactID. Hence different zero/empty threshold propositions each incur
one novel outcome charge, while exact replay and supported equivalent rewrites
incur zero incremental outcome charge.
\end{theorem}
\begin{proof}
The canonical payload in Eq.~\eqref{eq:outcome-fact} length-prefixes $h_N$,
$h_R$, and $m$. Unequal $h_N$ therefore yields unequal semantic payloads;
under the collision-detection assumption, it cannot reuse the same ledger
identity. Equal normalized plans and normalized releases make every payload
component equal. Novel-set settlement then gives charges one and zero,
respectively.
\end{proof}

\subsection{Closed Typed Query Language}

The complete core syntax is
\begin{align*}
e ::= {}& \operatorname{Scan}[\nu,s,r,\Gamma]
 \mid \sigma_{p;F}(e) \mid \pi_A(e) \mid e\bowtie_{\Theta}e \\
 &\mid e\cup_\delta e \mid \gamma_{G;\mathcal A}(e)
 \mid \operatorname{Page}_{O,K,o,l}(e),\\
p ::= {}& b \mid p\wedge b,\qquad b ::= a\;op\;c,\\
op\in {}&\{=,\ne,<,\le,>,\ge\}
 \cup\{\code{LIKE},\code{IN},\code{NOT IN}\},
\end{align*}
where $F$ is exactly the fields read by $p$, $\Theta$ is a nonempty
conjunction of equalities, $\cup_\delta$ is union-distinct, and
$\mathcal A\subseteq\{\code{COUNT(*)},\code{COUNT},\code{SUM},\code{MIN},
\code{MAX}\}$.  No implicit SQL construct is hidden in $e$: outer joins,
difference, negation beyond the listed scalar predicates, windows, recursion,
subqueries, volatile functions, and \code{UNION ALL} are not terms.

The typing judgment $\Pi\vdash e:\Gamma$ is partial: every operator requires a
typed child; predicates and projections may name only child fields; join
equalities require disjoint role-qualified schemas and identical canonical
types/collations; union branches require one identical distinctable schema;
grouping requires typed group keys and aggregates; and pagination requires a
proven total order. Undefined premises reject the plan. The complete inference
rules are provided in the supplementary material.

Here $\mathbb B_3=\{\mathsf T,\mathsf F,\mathsf U\}$ is PostgreSQL
three-valued logic.  Equality with either operand NULL is $\mathsf U$;
$\mathsf F\wedge x=\mathsf F$, $\mathsf T\wedge x=x$, and
$\mathsf U\wedge\mathsf U=\mathsf U$.  Selection retains only $\mathsf T$.
By contrast, grouping and distinct use the typed equivalence
$v\equiv_\tau w$ defined by \code{IS NOT DISTINCT FROM}, so NULL is equivalent
to NULL.  Predicate evaluation and ordering are PostgreSQL functions fixed by
$pg$ and $\mathcal C$, not Go approximations.

The closed scalar domain $\mathcal T$ contains exactly the canonical types
\code{smallint}, \code{integer}, \code{bigint}, \code{numeric}, \code{real},
\code{double precision}, \code{boolean}, \code{bytea}, \code{date},
\code{time without time zone}, \code{timestamp without time zone},
\code{timestamp with time zone}, \code{uuid}, \code{text}, \code{character},
\code{character varying}, and \code{jsonb}, with PostgreSQL
aliases mapped to these canonical names.  Integers retain their
16/32/64-bit ranges; exact numerics are encoded as reduced rationals and never
accepted through a binary-float conversion; floats use their IEEE width and
canonicalize NaN, infinities, and negative zero.  Temporal values have
PostgreSQL microsecond precision, \code{timestamptz} is UTC, and
\code{time with time zone} is excluded.  Text equality/order binds exact
deterministic collation name and actual version.  The text-preserving
\code{json} type is excluded: PostgreSQL retains insignificant whitespace,
object-key order, duplicate keys, and numeric lexical forms, while \code{jsonb}
has structural canonical equality \cite{postgres16json}.  Aggregate result
types are exactly PostgreSQL's:
\code{COUNT}$\to$\code{int8}, \code{SUM(int2/int4)}$\to$\code{int8},
\code{SUM(int8)}$\to$\code{numeric}, \code{SUM(numeric)}$\to$\code{numeric},
and \code{MIN/MAX}$\to$the input type.  \code{SUM(real)} and
\code{SUM(double precision)} are not admitted: IEEE-754 addition is
non-associative, so a floating-point SUM depends on the physical aggregation
order, which the typed bag and the normal form do not fix.  Any cast,
arithmetic, aggregate, witness-multiplicity, or canonical-range overflow makes
evaluation undefined and the gateway releases and settles nothing.

\subsection{Annotated Bags and Complete Output Keys}

A relation is $R=\langle\Gamma,\mathcal B,M,\prec?\rangle$, where $M$ is a
finite PostgreSQL bag implemented by unique row keys.  Equal value tuples remain
distinct bag occurrences when their keys differ; only union-distinct and group
quotient occurrences into classes.  An optional $\prec$ records a proven total
order.  A row is
$t=\langle k,\{a\mapsto c_a\},S_r,W_r,O_r\rangle$ and a cell is
$c_a=\langle v,\tau,S_a,W_a,x_a,\rho_a?\rangle$.  $S$ is a finite set of
snapshot-covered base facts, $W$ a finite multiset with positive stored
multiplicities (possibly empty), $x$ a canonical
data-independent lineage expression, and $\rho$ an optional already
materialized release fact.  Every operator preserves
\begin{equation}
 \operatorname{support}(W_r)=S_r,\qquad
 \operatorname{support}(W_a)=S_a.                 \label{eq:anninv}
\end{equation}
For annotations define same-proof composition
$\bigoplus_i(S_i,W_i)=(\bigcup_iS_i,\sum_iW_i)$ and alternative-proof
composition
$\bigsqcup_i(S_i,W_i)=(\bigcup_iS_i,\max_iW_i)$, with pointwise multiset
sum/max.  Thus $A\bigsqcup A=A$, while join fanout remains in $\bigoplus$.

Let
\begin{equation}
 \operatorname{RID}(R,t)=H(\code{relation-row},N(\Gamma),\mathcal B,t.k).
 \label{eq:rid}
\end{equation}
$H$ is domain-separated, typed, and length delimited. Scan uses the Catalog
entity key; unary operators preserve the child key; join hashes the sorted
immediate-child RIDs; union-distinct hashes the complete typed tuple; and group
hashes its canonical group key (or a distinct global token). Every derived Fact
also binds the merged source/snapshot bundle, expression, value, and witness.
Thus equal-looking results from different sources or derivations remain
distinct, while SQL aliases and caller-provided product names do not
participate. The complete output-key construction is in the supplement; a
same-hash unequal key is a validation error, not a row merge.

\subsection{Operator Evaluation and Effect Extraction}

Evaluation is the partial judgment $\Pi;D\vdash e\Downarrow R$; ``partial''
means that a failed premise yields no relation, effect, settlement, or release.
For compactness, $\alpha_r(t)=(S_r,W_r)$,
$\alpha_a(t)=(S_a,W_a)$, $\operatorname{aug}_F(t)$ replaces
$\alpha_r(t)$ by $\alpha_r(t)\oplus\bigoplus_{a\in F}\alpha_a(t)$, and cell
maps are otherwise copied.  In the derived rules a cell is written as the
5-tuple $c_a=\langle v_a,\tau_a,\alpha_a,x_a,\rho_a\rangle$: value, type, the
annotation pair $\alpha_a=(S_a,W_a)$ that expands to support and witness, the
normalized lineage expression, and the optional materialized release fact.
E-Scan alone displays the fully expanded base-cell 6-tuple; every other rule
constructs each output cell explicitly, so representation closure does not rely
on prose. Table~\ref{tab:effect-summary} retains the review-relevant accounting
choices; the complete inference rules, auxiliary functions, NULL/empty-input
cases, and page bounds are in the supplementary material.

\begin{table*}[t]
\centering
\caption{Main-text summary of the V2 big-step effect. ``Row annotation'' is
always included in the dependency set of a delivered row.}
\label{tab:effect-summary}
\small
\setlength{\tabcolsep}{4pt}
\begin{tabular}{L{0.12\textwidth}L{0.39\textwidth}L{0.42\textwidth}}
\toprule
Operator & Output construction & Positive-output dependency rule \\
\midrule
Scan & Keyed PostgreSQL bag; base cells retain typed values & Singleton base-row and base-cell FactIDs \\
Select/Project & Keep only $\mathsf T$ rows; restrict visible cells & Add every predicate field to retained row annotations; projection preserves them \\
Inner join & Every matching keyed pair under PostgreSQL equality & Union both row annotations and all join-key cell annotations; preserve fanout in witnesses \\
Union-distinct & Classes under componentwise \code{IS NOT DISTINCT FROM} & Include every candidate and every distinctness field, including hidden fields; max-merge alternative witnesses \\
\shortstack[l]{Group/\\aggregate} & One output per key class; global empty input yields one row & Include group-key inputs and every declared aggregate argument; \code{COUNT(*)} uses member rows, and NULL/non-extreme inputs remain dependencies \\
Page & Stable total-order slice; annotations copied & Account delivered rows only; non-output rows, rank, and order-only fields are outside the profile \\
\bottomrule
\end{tabular}
\end{table*}

Union is conservatively bag-aware and becomes idempotent only when its complete
typed tuples are proven duplicate-free. Grouping/distinct equivalence is
\code{IS NOT DISTINCT FROM}. Exact/integer \code{SUM} is admitted, but
floating-point \code{SUM} is rejected because the typed bag does not fix a
physical fold order. Pagination appends Catalog keys until the PostgreSQL order
is total. These restrictions make the summarized relation a partial function,
not a semantics for arbitrary SQL.

Finally, define materialization and the effect---they are not an implementation
afterthought:
\begin{align}
\operatorname{new}(R,t,a)&=
 f_d(\mathcal B,t.k,N(x_a),\tau_a,v_a,W_a),\\
\operatorname{mat}(R,t,a)&=
\begin{cases}
\rho_a,&\operatorname{valid}(\rho_a,R,t,a),\\
\operatorname{new}(R,t,a),&\text{otherwise},
\end{cases}\\
\facts_r(R,V)&=\{\operatorname{mat}(R,t,a)\mid t\in R,\ a\in V\},\\
\facts_i(R,V)&=\bigcup_{t\in R}S_r(t)\ \cup
                \bigcup_{t\in R,a\in V}S_a(t),\\
\rieffect_\Pi(e,D,V)&=\operatorname{normalize}
  \langle\facts_r(R,V),\facts_i(R,V)\rangle.       \label{eq:observe}
\end{align}
Here $\Pi;D\vdash e\Downarrow R$, and $\operatorname{valid}$ checks value,
type and bundle, plus support for a base fact or expression/witness for a
derived fact, against the pre-existing release fact.
Thus query bags and witness multiplicity are retained during composition, while
the two base ledgers are sets.  Equation~\eqref{eq:observe} is the declared
positive-output dependency profile: it is exact with respect to these
inductive rules, not with respect to arbitrary causal provenance or physical
source access.  Empty answers have an empty base effect except for the single
visible row of a global aggregate; V3 nevertheless adds one outcome fact.

\begin{theorem}[Representation closure and partial-function semantics]
For every admissible $\Pi$, typed finite $e$, and immutable $D$, evaluation has
at most one result.  If $\Pi;D\vdash e\Downarrow R$, $R$ satisfies
Eq.~\eqref{eq:anninv}, has canonical schema/bundle and unique row keys, and is a
valid input to every well-typed parent rule.
\end{theorem}
\begin{proof}
By structural induction on $e$.  Scan is fixed by $D$ and constructs singleton
support/witness pairs.  Select's three-valued predicate and dependency set are
fixed by $\Pi$; Select, Project, and Page only take a deterministic subbag or
copy annotations.  Join enumerates a uniquely determined bag of matching key
pairs; disjoint schemas, Eq.~\eqref{eq:rid}, and the pair key make each output
usable by another Join.  Union first collapses an identical annotated branch
only under the duplicate-free full-tuple premise, then has unique typed-tuple
equivalence classes and uses deterministic
same-proof union/addition within a candidate and alternative union/max across
candidates; E-Union constructs each output cell
$\langle v_a,\tau_a,\bigsqcup\alpha_a,N(\code{union}(x)),\rho_a\rangle$ from
those classes.  Group has unique not-distinct classes, the explicitly defined
empty class, deterministic exact/numeric sum folds (floating-point SUM is
excluded from the closed language, so no order-dependent fold arises), and one
complete PostgreSQL result per class; E-Group composes every group-key input
into the row annotation and constructs both group-key and aggregate cells from
that result.  Set union with multiset sum or max preserves equal
support.  Total order makes Page's slice unique.  Each case constructs the
claimed invariant, and the validator converts any range, collision, schema,
oracle, or multiplicity violation into no derivation.
\end{proof}

\begin{theorem}[Dependency determinism]
Fixed admissible $\Pi$, typed $e$, snapshot map $D$, and visible field set $V$
uniquely determine the positive-output dependency FactSet $\facts_i$, and hence
$\rieffect_\Pi(e,D,V)$, whenever evaluation succeeds.
\end{theorem}
\begin{proof}
The preceding theorem gives a unique $R$.  Equation~\eqref{eq:observe} ranges
over its unique keyed occurrences and fields.  $C_\tau$, expression
normalization, bundle/key construction, witness commitment, Fact
materialization, and final set normalization are deterministic functions.
Therefore both finite Fact sets, and hence their ordered canonical encodings,
are unique.
\end{proof}

\begin{property}[Group-key completeness]
For every delivered group $Q$, member $t\in Q$, and group field $g\in G$,
$S_g(t)\subseteq\facts_i$, including when $g\notin V$.
\end{property}
\begin{proof}
E-Group includes $\bigoplus_{g\in G}\alpha_g(t)$ in the output row annotation.
Projection preserves that row annotation, and Eq.~\eqref{eq:observe} includes
every delivered row's $S_r$ independently of $V$.
\end{proof}

\begin{property}[Distinct-dependency completeness]
For every delivered union-distinct class $Q$, candidate $t\in Q$, and schema
field $a\in\Gamma$, $S_a(t)\subseteq\facts_i$, including when $a\notin V$.
\end{property}
\begin{proof}
E-Union first composes every schema-field annotation into $\beta_\Gamma(t)$,
then alternative-composes all candidates into the output row annotation.
Alternative max changes multiplicity but its support is the union, which
Eq.~\eqref{eq:observe} includes.
\end{proof}

\begin{property}[Conservative aggregate input]
Every delivered aggregate cell includes its complete declared logical input:
all member row annotations for \code{COUNT(*)}, and every member argument cell
for \code{COUNT(a)}, \code{SUM(a)}, \code{MIN(a)}, and \code{MAX(a)}.  NULL and
non-extreme \code{MIN/MAX} inputs are not omitted.
\end{property}
\begin{proof}
This is the aggregate-cell clause of E-Group followed by
Eq.~\eqref{eq:observe}.  PostgreSQL's scalar fold may ignore NULL or retain only
an extreme value, but the V2 dependency rule deliberately ranges over the
complete argument bag.
\end{proof}

\subsection{Conservation Properties}

The following properties apply only to the operators and rewrites stated here.
They are not claims about all semantically equivalent SQL.

\begin{property}[Normalization]
Fact-set normalization is deterministic, idempotent, and commutative under
merge.
\end{property}
\begin{proof}
By Fact identity well-definedness, a validated canonical payload has one map
key; a same-digest unequal payload aborts rather than merges.  Normalization is
therefore finite-set insertion followed by lexical canonical-payload order.
Idempotence and commutativity follow from $A\cup A=A$ and $A\cup B=B\cup A$;
the fixed serialization order gives the same bytes in each case.
\end{proof}

\begin{theorem}[Restricted projection/selection invariance]
For $F\cup V\subseteq A$, selection followed by projection and projection
followed by selection have the same visible effect:
\[
 \rieffect_\Pi(\pi_A(\sigma_{p;F}e),D,V)=
 \rieffect_\Pi(\sigma_{p;F}(\pi_Ae),D,V).
\]
Reordering $A$ also preserves this normalized terminal effect.
\end{theorem}
\begin{proof}
Because $F\subseteq A$, both selection predicates read identical typed cells
and retain exactly the rows whose truth value is $\mathsf T$.  By E-Select each
retained row receives the same
$\alpha_r(t)\oplus\bigoplus_{a\in F}\alpha_a(t)$; by E-Project both retain the
same keys and cells in $A$.  Equation~\eqref{eq:observe} over $V\subseteq A$
therefore yields identical release and dependency sets.  Field traversal order
is absent from Fact payloads and removed by set normalization.
\end{proof}

\begin{definition}[Typed normal form $NF_\Pi$]
\label{def:nf}
$NF_\Pi$ is the total map from well-typed terms $e$ to canonical terms induced
by the following structure-recursive rewrites, each of which either preserves
$\effect_\Pi$ or is the identity: erase SQL aliases and derived-expression
display names; sort conjunction literals in $p$, dependency fields $F$,
projection lists, group keys $G$, and aggregate families $\mathcal A$ by their
stable role-qualified field IDs and canonical expressions; sort join operands
and equality predicates by their child digests; sort union operands by digest
and collapse an identical duplicate branch only when the child normal form
proves full-tuple distinctness, so $e\cup_\delta e\equiv e$ for that
set-valued $e$; sort
relation-level union operands before any enclosing operator; and map a stable
page suffix to its catalog-canonical spelling (entity-key or all-group-key
suffix).  $NF_\Pi(q_1)=NF_\Pi(q_2)$ iff the two canonical terms are syntactically
identical.  The artifact realizing $NF_\Pi$ is
\texttt{taskgate-query-normal-form-v3}, and it performs no outer-join,
arithmetic, window, subquery, or general SQL equivalence reasoning.  This
query-level normalizer is distinct from the Fact-set $\operatorname{normalize}$
of Property~(Normalization), which acts on the final ledgers.
\end{definition}

\begin{theorem}[Typed normal-form soundness]
For admissible $\Pi$, immutable snapshot map $D$, typed $q_1,q_2$, and the
same visible field set $V$,
\[
 NF_\Pi(q_1)=NF_\Pi(q_2)\Longrightarrow
 \effect_\Pi(q_1,D,V)=\effect_\Pi(q_2,D,V).
\]
This implication is not a completeness claim for arbitrary equivalent SQL.
\end{theorem}
\begin{proof}
The typed normalizer can erase only aliases, conjunction/set traversal order,
join operand and predicate order, union operand order, an identical duplicate
union branch whose child is proven full-tuple distinct, and alternate spelling
of the same stable page suffix. A Scan carries no such proof; Union and Group
produce it, Select/Page preserve it, and Project preserves it only for an
unchanged schema. Alias is
absent from derived expressions. Set normalization removes traversal order.
Join hashes sorted child identities and uses commutative $\cup/\oplus$.
Union classes are operand independent; same-proof member composition includes
all equivalence fields, while alternative max-merge is commutative and
idempotent.  An identical materialized fact is retained, and relation-level
union expressions are sorted before any downstream composition. Group keys and witness
commitments are sorted. The SQL compiler emits the same catalog-canonical page
suffix represented by the normal form. Each atomic rewrite therefore preserves
Effect; structural induction over well-typed contexts proves the result.
\end{proof}

\begin{theorem}[Base-ledger split/page/join conservation]
Merging release/dependency observations for disjoint projections or a complete
page partition equals observing the combined result. Overlapping pages and
repeated terminal observations charge each release/dependency FactID at most
once; join fanout changes witness multiplicity but not the union of dependency
support. Outcome facts remain per normalized proposition and intentionally do
not collapse a family of different page or threshold questions.
\end{theorem}
\begin{proof}
For $V=V_1\cup V_2$, distribute both unions in
Eq.~\eqref{eq:observe} over $V_1,V_2$; this gives
$\rieffect(R,V)=\operatorname{merge}(\rieffect(R,V_1),\rieffect(R,V_2))$.
E-Page copies each selected keyed row and cell, so a complete disjoint slice
partition distributes the same two unions over the row index set.  Overlap and
repetition disappear by set idempotence.  E-Join adds witness multiplicities
with $\oplus$, but Eq.~\eqref{eq:observe} takes their support sets; repeated
support membership consequently has dependency cardinality one.  (A changed
derived witness may still change its release Fact, as required.)
\end{proof}

V2 uses \code{taskgate-query-normal-form-v3}. Besides the rewrites above it
binds namespace, snapshot, lineage digest, canonical SQL types, collation
names/versions, PostgreSQL three-valued bag semantics, UTC, and exact numeric
mode. It deliberately performs no outer-join, arithmetic, window, subquery, or
general SQL equivalence reasoning.

\section{Online Accounting}
\label{sec:online}

Formal semantics and the independent oracle cover scan/select/project, group
with four aggregates, inner equijoin, union-distinct, and page. Online lowering
is narrower: one Catalog product, or the bounded two-input forms below, may feed
grouping; pagination is one-product only. PostgreSQL end-to-end tests execute
paired statements and derive V2 release/dependency observations for hidden keys, join fanout,
complete union members, downstream aggregates, and page withholding. The
supplement gives the four-way coverage matrix. ``Online'' never means arbitrary
SQL containing the operator is accepted.

\subsection{Compilation and Paired Execution}

An exposure-enabled grant accepts either a one-product structured plan or one
closed two-input form.  The latter is (i) an inner join of two distinct
Catalog-stable roles with one or more same-type, same-collation equalities, or
(ii) union-distinct over two filtered aliases of one product, with the complete
deduplication schema explicit even when final $V$ hides some of its fields.
Nested join/set trees, self-join roles, union-all, and relational pagination
fail closed.  Callers cannot supply SQL fragments, FactIDs, provenance, or
semantic role identities.  Branch aliases are syntactic; join and union output
roles must match Catalog identities.

The compiler validates fields, predicates, grouping, and one of four
aggregates; \code{COUNT(*)} and \code{COUNT(field)} have distinct normal forms.
It produces two SQL statements from the same typed plan. A join companion
returns each positive matched row pair, including both entity keys and every
projected/join/filter/group/aggregate field. The visible and companion statements share
a stable pair-key order when the ordinary row ceiling can cut the bag. A
union-distinct visible statement eliminates duplicates over its complete
declared schema, whereas its companion deliberately uses \code{UNION ALL} plus
a branch marker and both source entity keys. Consequently every member of each
delivered distinct class remains reconstructible; selecting one representative
would be rejected as incomplete evidence. Grouped and membership-expanded
companions request one row beyond their evidence ceiling to detect truncation.
All generated statements pass the same PostgreSQL AST authorization and
mandatory-scope injection policy.

After execution, the gateway reconstructs Catalog-bound Scan annotations,
retains only provenance-certified join pairs or delivered union classes, and
invokes the same \code{JoinOnV2}, \code{UnionDistinctV2},
\code{AggregateFromResultsV2}, and \code{ObserveV2} implementation tested by
the algebra oracle. V3 then attaches the plan-normal-form and released-result
commitment from Eq.~\eqref{eq:outcome-fact}. It does not maintain a second ad hoc online dependency
rule. Hidden SQL output aliases are server-generated hashes and are mapped back
to stable field IDs before effect derivation and external delivery.

The connector executes both statements in one explicit read-only
\code{REPEATABLE READ} transaction \cite{postgres16transaction}. This prevents
a concurrent update from making output and provenance refer to different
database states. The connector reports business and provenance execution time
separately, while resource settlement charges their total. Truncation of the
provenance result is a hard error.

\subsection{Atomic Settlement}

For one successful exposure-enabled query, the gateway performs the following
steps (Fig.~\ref{fig:architecture-settlement}b):

\begin{enumerate}
\item Authorize the fixed plan, reserve ordinary and exposure budgets, then
execute and buffer its visible and provenance statements in one Business
PostgreSQL \code{REPEATABLE READ} snapshot.
\item Derive its release and positive-output-dependency observation, attach one
normalized outcome fact (including for empty/zero answers), and reject
truncated or inconsistent evidence.
\item In Control PostgreSQL, lock the query reservation and root ledger and
insert the observation's facts under a primary key on root, ledger kind, and
fact hash; conflicts contribute zero novel facts.
\item Conditionally increment all three used counters only if all limits remain
valid.
\item In the same transaction, settle query/row/database-time resources, store
the encrypted buffered result, append terminal audit evidence, and persist a
signed V5 receipt (V4 remains the backward-compatible two-ledger format).
\item Commit and only then return the buffered result.
\end{enumerate}

If the conditional budget update in Step 4 affects no row, the transaction
returns an exposure-budget error.
Fact inserts and result storage roll back, and the caller never receives the
buffer. PostgreSQL row locking serializes concurrent children at the shared
root. Unique fact keys make charge equal to successful novel inserts, rather
than a count computed before a race.

Let $n$ be the total number of unique release, dependency, and outcome facts in one
observation. Hashing is linear in encoded input size and normalization sorts at
most $n$ hashes, giving $O(n\log n)$ in-memory work and $O(n)$ memory. Ledger
inserts are chunked; index maintenance adds database-dependent
$O(n\log |\accounted_T|)$ work under a conventional balanced index model. These are
complexity bounds, not measured latency claims.

\subsection{Receipts and Failure Semantics}

A V5 Ed25519 receipt binds the root task, profile, actual and charged counts in
all three dimensions, and the SHA-256 of the normalized observation, in addition to
authorization, datasource, resource budget, result, and audit evidence. It
does not expose raw values or FactIDs.

Failures before connector invocation release both reservations. Ambiguous
connector outcomes conservatively charge the ordinary resource reservation but
release exposure because no result can be delivered before successful
settlement. A process restart applies the same rule. Terminal request replay is
observational: it returns the first persisted status or result and never starts
a second execution.

Withholding the buffer does not hide the decision bit. Whether settlement
accepts a query reveals whether its data-dependent novel Effect fits the
remaining budgets, and a rejected Effect is not itself inserted into either
ledger. Thus rejection can act as an uncharged threshold oracle. The current
protocol is not a simulatable auditor and makes no denial-noninterference claim.

\section{Delegation}
\label{sec:delegation}

\subsection{Root-Family Conservation}

A child authorization signs both \code{root_task_id} and
\code{parent_task_id}. Its human subject, catalog, datasource, schema, and
profile remain fixed, while products, columns, scopes, sensitivity, expiry, and
all budget dimensions may only narrow. Every execution revalidates the signed
parent grant and active ancestor chain.

All descendants resolve to one root ledger. If a parent and child request the
same fact, only the first successful settlement inserts it. Revoking the root
blocks new descendant queries, while an already terminal request ID remains
readable. Consequently, creating workers or task nodes cannot create fresh
exposure capacity. A child that signs a lower exposure ceiling may add novel
facts only while the shared family total remains within that ceiling; a
zero-novelty replay remains admissible.

This conservation scope stops at the root boundary. A second root task for the
same principal has an independent ledger and therefore a fresh budget if a
human approves it. The prototype has no principal-, tenant-, or organization-
wide exposure ledger, so neither delegation monotonicity nor the theorem below
prevents cross-root accumulation.

\begin{theorem}[Atomic root-family budget safety]
After any committed settlement sequence, each root ledger equals the union of
the novel charges of its settled family queries, and no ledger cardinality
exceeds its root limit.
\end{theorem}
\begin{proof}[Proof sketch]
All family settlements lock the same root row. The unique fact key admits one
first insert for each root/kind/hash tuple. A transaction adds exactly its
successful inserts to the counters, checks all limits, and otherwise rolls
back. Induction over the resulting serial commit order establishes the union
identity and all three bounds.
\end{proof}

\textbf{Budget calibration.} The three ceilings $B^r_T,B^i_T,B^o_T$ are capacity
declarations, not learned risk scores. In our workflow-first procedure the
approver fixes fields, scope, sensitivity, and useful questions; a dry run on
an approved representative snapshot (or conservative cardinality bound) unions
the FactSets of alternatives, pages, and delegates. From the resulting
$U(B^r,B^i)$ frontier, the approver selects the smallest release/dependency
ceiling meeting required workflow completion and records data-growth headroom;
$B^o$ separately caps distinct successful propositions (often at the approved
query count). Hence 1,000
means capacity for 1,000 distinct semantic facts, not ``risk 1,000.''
Section~\ref{sec:rq5} instantiates this procedure.

Unit weights provide representation-independent integers, exact replay/delegation
set union, and stable audit thresholds. They do not equate harm: field and
sensitivity policy remains the first boundary. A harm model may maintain a
separate sensitivity-weighted ledger over the same FactIDs; neither that model
nor differential-privacy composition is claimed here.

\section{System Realization}
\label{sec:system}

The prototype is implemented in Go with separate control and business
PostgreSQL 16 databases. A logical catalog declares reporting views, mandatory
scopes, stable snapshots, entity keys, allowed operations, approval routes, and
resource/exposure profiles. Startup and per-query attestation bind datasource
identity, database, role, PostgreSQL major version, ordered view schema, and
normalized view definitions.

Clients use MCP tools to list products, request a task, execute one structured
plan, inspect both budgets, delegate a narrower task, and retrieve results or
receipts. An external approval adapter
signs the immutable grant. Arbitrary direct SQL remains available only for
legacy resource-only grants; it fails closed when exposure evidence is
required.
The wire and storage label \code{influence} is retained for compatibility;
under the V2/V3 profiles it denotes positive-output dependency, not causal
influence.

The control schema stores one three-dimensional ledger per root, per-query reservations, and
immutable fact identities. The primary-key and check constraints are backed by
application transactions and update/delete rejection triggers. Result rows are
encrypted with AES-GCM; audit events form a hash chain. These mechanisms are
orthogonal to exposure semantics but make settlement and later verification
observable.

\section{Finite-State Analysis}
\label{sec:formal}

The split TLA+ model \code{ExposureLedger.tla} contains one root, one child, two
requests, two facts in each ledger universe, all three limits set to one, and one
terminal replay. Its transitions are reserve, execute/buffer, derive,
settle, over-budget reject, pre-execution release, replay, and root revocation.
It checks three-budget safety, exact novel charge, no delivery before settlement,
no charge without settlement, buffered rejection, evidence/buffer agreement,
non-overlapping novel charges, task-family non-amplification, and at-most-once
physical execution for delivered requests.

Using pinned TLC 1.7.1, the provenance-checked run generated
\ExposureFormalStates{} states, found \ExposureFormalDistinct{} distinct states,
and reached depth \ExposureFormalDepth{} with no invariant error. Model,
configuration, and raw-log digests are validated before this manuscript builds
\cite{lamport2002specifying}.

This is exhaustive only for the submitted finite configuration. The model
assumes exact fact derivation, abstracts SQL bytes and cryptographic hashes, and
does not mechanically prove that Go/PostgreSQL refines TLA+. The repository
therefore includes an action-to-method-to-transaction-to-test refinement map
rather than claiming a proof that does not exist.

\section{Evaluation}
\label{sec:evaluation}

\subsection{Questions and Reproducibility}

The evaluation is organized around five questions: RQ1 ground-truth accuracy;
RQ2 rewrite invariance of exposure accounting; RQ3 resistance to split, page,
retry, zero/empty threshold probing, snapshot, join/union, delegation, and concurrency amplification; RQ4 runtime/storage
overhead and cost scaling; and RQ5 policy-calibration worked examples. The report
is rebuilt for \ExposureProfile{} from a source-controlled V2 base-algebra corpus (V3 preserves
those release/dependency FactIDs) with digest prefix
\ExposureCorpusHash{}. The primary RQ2 evidence is a closed-language
FactSet-invariance campaign; a separate broad SQL result-equivalence stress test
is reported only in the supplement and is not exposure evidence. RQ4 contains \RQFourTrials{} fresh
isolated PostgreSQL deployments, \RQFourFullPathOperations{} full-path calls and
\RQFourAblationOperations{} direct/paired/algebra ablation measurements
(\RQFourObservations{} total), and
\RQFourScalingDims{} deterministic in-process scaling sweeps, plus
\RQFourStorageTrials{} fresh-schema production-ledger scaling trials and a
public-endpoint PostgreSQL Join--Group campaign with \RQFourScalePoints{}
cardinalities and \RQFourScaleTrials{} fresh-ledger trials per cardinality.
There is no online representation-selection planner: one client-supplied plan
is executed per call. RQ5 exhaustively enumerates only the eight subsets of
each three-goal policy fixture to expose an approval frontier; it is not a
runtime planner, a live-agent experiment, or a learned utility model.

\begin{table}[t]
\centering
\caption{Current source-backed evidence.}
\label{tab:current-evidence}
\small
\begin{tabular}{L{0.12\columnwidth}L{0.70\columnwidth}}
\toprule
RQ & Current result \\
\midrule
RQ1 & \RQOnePassed{}/\RQOneCases{} independent-oracle cases on \RQOneRows{} source rows \\
RQ2 & \RQTwoExposureRewrites{} closed-language rewrite families, \RQTwoExposureCases{} instances and \RQTwoExposureNFCases{} normal-form checks: FactSet/charge invariant, \RQTwoExposureMismatches{} mismatches \\
RQ3 & \RQThreePassed{}/\RQThreeCases{} deterministic; \RQThreeIntegration{}/\RQThreeExecuted{} PostgreSQL race tests; paired PG Join/Union exchange and downstream-group tests \\
RQ4 & \RQFourFullPathOperations{} small-fixture full-path calls plus \RQFourAblationOperations{} ablations; physical ledger curve to \RQFourStorageMaxRows{} rows; end-to-end Join--Group to \RQFourScaleMaxJoined{} members/\RQFourScaleMaxFacts{} dependencies \\
RQ5 & \RQFiveScenarios{} illustrative policy fixtures and \RQFiveGoals{} author-declared binary goals; exact release/dependency budget--utility slices and fact-class calibration \\
\bottomrule
\end{tabular}
\end{table}

RQ1 uses 12 expense and four department rows and covers projection, positive
and empty selections, TRUE/FALSE/UNKNOWN boundaries, a hidden group key, a
hidden union-distinct field, \code{COUNT(a)} with NULL, \code{MIN/MAX} with
non-extreme inputs, join, and page/order boundaries. A separate reference
package imports none of the production exposure,
query-plan, gateway, or policy code; it independently constructs the complete
semantic Fact objects, witness commitments, and hashes. The production output
matches its \RQOneReleaseFacts{} release and \RQOneInfluenceFacts{} dependency
case-level fact memberships, with per-case set digests retained in the report.
RQ2 measures rewrite invariance of the accounting itself, not merely of query
results. We express each side of
\RQTwoExposureRewrites{} closed-language rewrites (selection/projection
reordering, conjunction reordering, projection and visible/hidden group-key
field ordering, join operand and predicate reordering, union operand exchange,
union-distinct idempotence, and duplicate branch collapse) as an algebra term
over \RQTwoExposureDatasets{} data instances
and snapshots, and require, for each pair, that the typed normal form agrees
(where the rewrite is NF-canonical), that the release and dependency FactSets are
identical, and that the incremental charge is zero in both directions. The
campaign reports \RQTwoExposureCases{} instances, \RQTwoExposureNFCases{}
normal-form checks and \RQTwoExposureEffectChecks{} bidirectional Effect checks with
\RQTwoExposureMismatches{} mismatches. The ten families are not ten datasets:
they cross five small, source-controlled data instances.  The supplementary
SQL campaign is kept separate because most of its templates lie outside the
closed algebra and it checks rows, not FactSets.

RQ3 deterministically checks join multiplicity in the annotated algebra and
checks split/merge, overlapping pages, cache/retry, and snapshot update. One
online PostgreSQL test compiles and executes both paired statements for join
and union operand exchange, verifies identical observation digests, and
composes each operator with downstream grouping and aggregation. Its union
companion must expose both branch members and the hidden full-tuple field; its
join companion preserves actual fanout pairs. A second test enters through the
public \code{execute_plan} API, executes against Business PostgreSQL, settles
all ledgers in Control PostgreSQL, and verifies that internal fields are
stripped before release. These two tests plus delegation and concurrent
root-ledger settlement run with the Go race detector; one artifact binds all
named test-pass and package-pass events to the SHA-256 of the raw
\code{go test -json} log.

The V3 outcome-probing case issues \RQThreeThresholdQuestions{} distinct
global-count predicates whose answer is zero and whose release/dependency
increments after the first are both zero. They produce
\RQThreeDistinctOutcomes{} distinct outcome FactIDs and
\RQThreeOutcomeCharges{} novel outcome charges. Repeating the first predicate
charges \RQThreeReplayOutcomeCharge{}, and an alias/case/whitespace rewrite
with the same normal form charges \RQThreeRewriteOutcomeCharge{}. This is an
anti-collapse test, not a claim that unit outcome cost equals semantic
information content.

\subsection{Descriptive Baseline Contrast}

For one grouped sum/count fixture, Table~\ref{tab:baselines} reports six
descriptive accounting baselines. Their units differ, so the table is not a
ranking. It demonstrates why row count alone misses dependency facts and why
history is required for zero incremental replay.

\begin{table}[t]
\centering
\caption{Charges for one auditable fixture, not performance results.}
\label{tab:baselines}
\small
\begin{tabular}{lr}
\toprule
Baseline & Charge \\
\midrule
Query count & \BaseQueryCount{} \\
Returned rows & \BaseRows{} \\
Serialized bytes & \BaseBytes{} \\
Weighted cells & \BaseCells{} \\
Provenance without history & \BaseNoHistory{} \\
Full, first execution $(r,i)$ & \BaseFirst{} \\
Full, replay $(r,i)$ & \BaseReplay{} \\
\bottomrule
\end{tabular}
\end{table}

\subsection{RQ4: End-to-End Overhead and Contention}

RQ4 uses the dedicated public-\code{execute_plan} exposure harness. The separate
four-baseline TPC-derived runner calls legacy resource-only \code{query_sql}; it
is labeled \code{resource_taskgate}, is not used below, and cannot substantiate
an exposure/provenance overhead claim.

The recorded scale/performance campaigns exercise the V2 release/dependency
subledger, which is the dominant materialized set and is byte-for-byte retained
inside V3. V3 functional and race tests additionally execute the outcome insert
and V5 receipt, but this artifact does not isolate its one-fact latency at every
scale. We therefore do not attribute the published RQ4 timings to a separately
measured V3 outcome overhead.

The controlled local campaign uses an i9-13900HX host with 32 logical CPUs and
16.6~GB RAM under WSL2/Docker 29.1.3. Each of three trials creates fresh
Business and Control PostgreSQL~16.14 databases, a gateway, one root task, and
eight delegated workers. After a 32-query history ramp, each worker issues 200
measured warm-cache queries at concurrency 1, 4, and 8. Every full-path request
enters through public MCP and includes authorization, reservation, paired
visible/provenance execution, effect derivation, exact history lookup,
transactional settlement, encryption, audit, and receipt signing. Results use
the median of trial summaries; each trial percentile is Hyndman--Fan type~7.

At concurrency one, median p50 latency rises from
\RQFourDirectOneMedian{}~ms for Business PostgreSQL alone, through
\RQFourPairedOneMedian{}~ms for the paired snapshot and
\RQFourAlgebraOneMedian{}~ms with effect derivation, to
\RQFourFullOneMedian{}~ms for the complete path, a
\RQFourFullOneRatio{}$\times$ p50 multiplier over the direct query. Across the
three fresh deployments, direct-query p95 ranges from
\RQFourDirectOneTailLow{} to \RQFourDirectOneTailHigh{}~ms and full-path p95
ranges from \RQFourFullOneTailLow{} to \RQFourFullOneTailHigh{}~ms; these
trial ranges are the uncertainty bars, rather than treating operations within
one deployment as independent replicates. Table~\ref{tab:rq4} shows the
full-path scaling result. Throughput saturates near 170 queries/s: the shared
root ledger intentionally serializes descendants, and its p95 lock component
grows from \RQFourLockOneTail{} to \RQFourLockEightTail{}~ms. This identifies
settlement, rather than provenance execution, as the dominant bottleneck at
high concurrency.

\begin{table}[t]
\centering
\caption{Warm full-path history-hit performance; median over three trials.}
\label{tab:rq4}
\small
\begin{tabular}{rrrrr}
\toprule
$c$ & p50 ms & p95 ms & query/s & lock p95 ms \\
\midrule
1 & \RQFourFullOneMedian{} & \RQFourFullOneTail{} & \RQFourFullOneQPS{} & \RQFourLockOneTail{} \\
4 & \RQFourFullFourMedian{} & \RQFourFullFourTail{} & \RQFourFullFourQPS{} & \RQFourLockFourTail{} \\
8 & \RQFourFullEightMedian{} & \RQFourFullEightTail{} & \RQFourFullEightQPS{} & \RQFourLockEightTail{} \\
\bottomrule
\end{tabular}
\end{table}

The 32-request ramp is not a 100\%-hit workload: it contains
\RQFourNovelSamples{} novel and \RQFourRampHitSamples{} hit observations across
the trials (\RQFourRampHitRate{}\% query-history hit rate). The first full-path
request against each fresh ledger has median \RQFourFreshNovelMedian{}~ms and
range [\RQFourFreshNovelLow{},\RQFourFreshNovelHigh{}]~ms; all novel ramp
requests have p50 \RQFourNovelMedian{}~ms with per-trial p50 range
[\RQFourNovelLow{},\RQFourNovelHigh{}]~ms, versus
\RQFourRampHitMedian{}~ms [\RQFourRampHitLow{},\RQFourRampHitHigh{}] for ramp
hits. This is a fresh-ledger/novel comparison, not a PostgreSQL buffer-cache
cold-start claim.

The ramp inserts \RQFourRampFacts{} immutable facts (12 release and 16
dependency facts) in every trial. Their canonical identity/payload occupies
\RQFourPayloadBytes{} bytes (\RQFourPayloadPerFact{} bytes/fact); observed
PostgreSQL allocation grows by \RQFourTableBytes{} table bytes and
\RQFourIndexBytes{} index bytes, \RQFourAllocatedBytes{} bytes in total.
Because PostgreSQL allocates pages, the allocated-per-fact quotient is not a
marginal storage estimate. All subsequent dedicated hit queries have 100\%
fact- and query-history hit rates and zero ledger-row growth, confirming that
replay cost is processing overhead rather than repeated exposure charge.

To measure ledger growth beyond the ten-row Business fixture, a separate
campaign invokes the production Control PostgreSQL reservation and atomic
settlement code in \RQFourStorageTrials{} fresh migrated schemas. In each trial,
one root ledger grows cumulatively through 10, 100, 1,000, and
\RQFourStorageMaxFacts{} release facts and the same number of dependency facts.
Each novel cumulative observation is followed by a new-query replay with the
identical FactSets. Table~\ref{tab:rq4-storage} reports medians and includes both
canonical identity/payload bytes and page-granular PostgreSQL table-plus-index
allocation.

\begin{table}[t]
\centering
\caption{Production Control PostgreSQL ledger scaling; three fresh schemas.}
\label{tab:rq4-storage}
\small
\resizebox{\columnwidth}{!}{%
\begin{tabular}{rrrrrr}
\toprule
facts/ledger & rows & payload MiB & alloc. MiB & novel ms & replay ms \\
\midrule
10 & 20 & \RQFourStorageTenPayload{} & \RQFourStorageTenAllocated{} & \RQFourStorageTenNovel{} & \RQFourStorageTenReplay{} \\
100 & 200 & \RQFourStorageHundredPayload{} & \RQFourStorageHundredAllocated{} & \RQFourStorageHundredNovel{} & \RQFourStorageHundredReplay{} \\
1,000 & 2,000 & \RQFourStorageThousandPayload{} & \RQFourStorageThousandAllocated{} & \RQFourStorageThousandNovel{} & \RQFourStorageThousandReplay{} \\
10,000 & 20,000 & \RQFourStorageTenThousandPayload{} & \RQFourStorageTenThousandAllocated{} & \RQFourStorageTenThousandNovel{} & \RQFourStorageTenThousandReplay{} \\
\bottomrule
\end{tabular}
}
\end{table}

At the largest point, novel settlement is \RQFourStorageTenThousandNovel{}~ms
with trial range [\RQFourStorageTenThousandNovelLow{},
\RQFourStorageTenThousandNovelHigh{}] and 100\%-overlap replay is
\RQFourStorageTenThousandReplay{}~ms
[\RQFourStorageTenThousandReplayLow{},
\RQFourStorageTenThousandReplayHigh{}]. Replay charges and grows by zero, but
is not constant-time: the production collision guard verifies every submitted
fact payload, even though the implementation fetches hashes in fixed-size
batches. This result exposes a real high-overlap bottleneck and motivates a
safe authenticated summary or index; we do not hide it behind the one-row
latency. Finally, all \RQFourBudgetBoundaryPassed{} trials submit
10,001 facts per ledger against a 10,000-fact signed ceiling, are rejected, and
remain at \RQFourStorageMaxRows{} rows, testing budget-boundary sensitivity and
atomic rollback.

\subsection{RQ4: Multi-Scale Join--Group End-to-End}

The preceding campaigns isolate request overhead and ledger storage; neither
executes a large relational result through effect derivation and settlement.
We therefore add a deterministic PostgreSQL~16.14 workload with the TPC-H
Orders--Lineitem relationship shape. It is TPC-H-derived, not an official TPC-H
benchmark: rows come from fixed \code{generate\_series} formulas, and we do not
claim the DBGen distributions, query suite, power, or throughput metrics. The
materialized base contains 50,000 orders and 250,000 lineitems. Three fresh root
tasks at each of 1,000, 10,000, and 45,000 orders enter through public
\code{execute\_plan}. The plan equijoins Orders and Lineitem, filters by the
scale boundary, groups by a three-valued order status, and returns
\code{SUM(extendedprice)}, \code{SUM(linenumber)}, and \code{COUNT(*)}. A direct
SQL call immediately precedes the novel call and its distinct-request replay;
services are warm, while ledger state is fresh per scale/trial.

The five-to-one fanout yields five joined members per order. Under the declared
semantics, set deduplication leaves three dependency facts per order (row,
join-key cell, and status cell) and four per lineitem (row, join/filter key,
price, and line number), exactly $23N$. Each output has three groups and four
visible fields, hence 12 release facts. Every novel observation in all nine
tasks charges exactly those expected sets. Every replay has the same observation
digest, charges zero facts, and leaves the ledger-row count unchanged.
Figure~\ref{fig:rq4-scale} reports medians over three independent fresh-ledger
trials; direct SQL is in milliseconds and full-path calls are in seconds.

\begin{figure}[t]
\centering
\begin{tikzpicture}
\begin{loglogaxis}[
  width=\columnwidth,
  height=5.0cm,
  xmin=18000, xmax=1300000,
  ymin=0.003, ymax=260,
  xlabel={dependency facts},
  ylabel={median latency (s)},
  xtick={23000,230000,1035000},
  xticklabels={23k,230k,1.035m},
  grid=both,
  minor grid style={gray!12},
  major grid style={gray!28},
  legend style={font=\scriptsize, at={(0.02,0.98)}, anchor=north west,
    draw=none, fill=white, fill opacity=.85, text opacity=1},
  tick label style={font=\scriptsize},
  label style={font=\scriptsize}
]
\addplot+[black, dashed, mark=triangle*, mark size=1.8pt]
  coordinates {(\RQFourScaleLowFactsPlot,\RQFourScaleLowDirectS)
    (\RQFourScaleMidFactsPlot,\RQFourScaleMidDirectS)
    (\RQFourScaleHighFactsPlot,\RQFourScaleHighDirectS)};
\addlegendentry{direct SQL}
\addplot+[blue!70!black, semithick, mark=*, mark size=2pt]
  coordinates {(\RQFourScaleLowFactsPlot,\RQFourScaleLowNovelS)
    (\RQFourScaleMidFactsPlot,\RQFourScaleMidNovelS)
    (\RQFourScaleHighFactsPlot,\RQFourScaleHighNovelS)};
\addlegendentry{novel}
\addplot+[orange!85!black, semithick, mark=square*, mark size=2pt]
  coordinates {(\RQFourScaleLowFactsPlot,\RQFourScaleLowReplayS)
    (\RQFourScaleMidFactsPlot,\RQFourScaleMidReplayS)
    (\RQFourScaleHighFactsPlot,\RQFourScaleHighReplayS)};
\addlegendentry{replay}
\end{loglogaxis}
\end{tikzpicture}
\caption{PostgreSQL Join--Group end-to-end scaling (median of three fresh
trials). Exact materialization makes both novel and zero-charge replay grow
sharply with dependency-set size.}
\label{fig:rq4-scale}
\end{figure}

At the million-fact point, novel latency is \RQFourScaleHighNovelS{}~s
[\RQFourScaleHighNovelLowS{}, \RQFourScaleHighNovelHighS{}] and replay latency
is \RQFourScaleHighReplayS{}~s [\RQFourScaleHighReplayLowS{},
\RQFourScaleHighReplayHighS{}]. Median effect derivation alone consumes
\RQFourScaleHighNovelDeriveS{}~s novel and
\RQFourScaleHighReplayDeriveS{}~s replay; fact-store work consumes
\RQFourScaleHighNovelStoreS{} and \RQFourScaleHighReplayStoreS{}~s,
respectively, versus \RQFourScaleHighDirectMS{}~ms for direct SQL. Campaign-wide
peaks are \RQFourScaleGatewayGiB{}~GiB for the gateway and
\RQFourScaleControlGiB{}~GiB for Control PostgreSQL. Thus the complete design
does execute more than one million dependency facts with exact replay
accounting, but the result is not operationally attractive: materialized
FactSets and repeated derivation dominate, and current latency/memory require
streaming derivation, compact set representations, or incremental authenticated
history before low-latency deployment.

Three deterministic in-process sweeps separately scale effect derivation,
normal-form depth, and novel-versus-replay deduplication. At
\RQFourScalingMaxRows{} source/evidence rows, derivation takes
\RQFourScalingObserveMicros{}~$\mu$s; at \RQFourScalingNovelMax{} ledger facts,
replay charges \RQFourScalingReplayCharge{}. These isolate cost growth but are
not substitutes for the PostgreSQL end-to-end result.

\subsection{RQ5: Policy-Calibration Worked Examples}
\label{sec:rq5}

RQ5 instantiates finance summary, overlapping-page case review, and delegated
exception-escalation policies on the 16-row auditable fixture. Each declares
three binary goals; $U$ is their admitted fraction, not human-rated, model, or
agent success. We enumerate each scenario's eight subsets and report the best
feasible $U$ at 25, 50, 75, and 100\% of its full release/dependency ceiling,
with the outcome ceiling fixed above the three goal propositions. The digest-checked
manifest identifies sensitive fields and aggregate steps.

The full ceilings are \RQFiveFinanceFull{}, \RQFiveReviewFull{}, and
\RQFiveDelegationFull{}; dependency row/ordinary/sensitive/aggregate-support
counts are \RQFiveFinanceClasses{}, \RQFiveReviewClasses{}, and
\RQFiveDelegationClasses{}. At 25/50/75/100\% of each full release/dependency ceiling, best
utility is 0/33/66/100\%; the supplement gives every $(B^r,B^i)/U$ point.
Finance's $(5,9)$ quarter-budget admits nothing, demonstrating why an
uncalibrated ``10'' has no governance meaning.

As a non-admission sensitivity analysis, weighting tagged cells five changes
the three total costs from 53/60/68 to 53/156/192. This illustrates the
separate risk-ledger option; the small fixture demonstrates calibration, not
transferable organizational thresholds.

To avoid relabeling that evidence, the artifact separately preregisters a
\emph{designed, not collected}, participant-free controlled Agent benchmark on
business-motivated synthetic tasks. Six calibration tasks run unbudgeted in
three fresh conversations each (18 total); 12 separate tasks then register 576
budgeted workflows (four policies, four levels, three replicates) and 60
unbudgeted references. Only task identifiers, objectives, and scoring roles are
held out; both phases use the exact frozen snapshot. Calibration admits only
completed, complete-envelope runs with query evidence and claims no answer
correctness. Per-domain, component-wise lower-median usage is scaled to
25/50/75/100\%, floored at one, and digest-frozen before evaluation.
Operational sanity caps---Facts $10^6/10^7/10^3$ and queries/rows/bytes
$10^2/10^5/10^8$---invalidate over-bound calibration rather than define budgets.
DeepSeek uses fresh database, root, cache, and conversation state. A
source-locked, policy-blind oracle separates evidence-eligible answer score,
trace guards, and combined workflow score. The primary comparison is answer
score versus sensitivity-weighted semantic exposure of admitted queries;
dependencies need not be displayed. Uniform cross-policy checks include
V3-derived sensitive-record/field/cell/value/outcome views and query/row/byte
volume counters, but the former are not representation-neutral. Volume counters
are reconstructed only from responses admitted to the Agent; the gateway's
separate operational ledger can additionally include an executed buffered result
that a baseline or semantic budget rejects before release. We report no
outcome from the 18 calibration or 636 evaluation workflows. Even when
collected, this cannot establish human budget comprehension, approval usability,
empirical representativeness, organizational adoption, or production utility.

\subsection{Reproducibility Boundary}

RQ1/RQ2 use 16 source rows and \RQTwoExposureDatasets{} small data instances;
online coverage uses a ten-row fixture. RQ4 adds real Control PostgreSQL storage
to \RQFourStorageMaxRows{} fact rows and a three-scale, public-path Join--Group
campaign ending at \RQFourScaleMaxJoined{} joined members and
\RQFourScaleMaxFacts{} dependencies. It is still one warm, deterministic,
TPC-H-derived shape on one host with three repeats, not an official suite,
cold-cache, multi-node, or production-SLO study. RQ5 measures declared goal
admission on the 16-row fixture, not human utility or agent success; the
participant-free controlled benchmark is infrastructure rather than evidence
until its frozen 654-workflow collection is complete, and would remain bounded
to business-motivated synthetic tasks, one frozen snapshot, and a recorded
hosted-model alias whose provider-side weights may drift. The build
parses every published RQ4 JSONL sample, reproduces summaries, and recomputes
source digests. The controlled-benchmark lock can detect mixing or later drift
among retained artifacts, but, absent an external append-only anchor, cannot
prove that no calibration output was removed or replaced before budget
freezing; reproducibility does not remove these external-validity and
collection-integrity bounds.

\section{Limitations and Threats to Validity}
\label{sec:limits}

\textbf{Semantic scope.} V3 accounts each successful normalized proposition,
including an empty relation or zero scalar, but assigns one unit rather than
estimating its information content. It does not enumerate rejected rows as
dependencies or account non-output groups/pages, order/rank-only inputs,
timing, correlations, background knowledge, or physical reads. A single rich
proposition may reveal more than one bit. The mechanism is neither minimal
causal provenance nor differential privacy.

\textbf{Decision and subject scope.} Refusal exposes an uncharged threshold bit;
no denial noninterference is claimed. Ledgers cover one root family, so repeated
approval creates fresh capacity unless an external principal/tenant policy
prevents it.

\textbf{Query and identity scope.} Online support is one product or a bounded
two-role inner equijoin/one-product union-distinct, optionally grouped; nested
trees, self/outer joins, union-all, relational pagination, windows, negation,
recursion, and arbitrary subqueries fail closed. Stable entity keys and snapshot
labels are administrator declarations; production requires versioned
publication/CDC and collision review.

\textbf{Cost and deployment.} Exact FactSets, root locking, and immutable
history are expensive: the million-fact result is
\RQFourScaleHighNovelS{}/\RQFourScaleHighReplayS{}~s novel/replay and
\RQFourScaleGatewayGiB{}~GiB gateway memory. Streaming, compression,
partitioned settlement, retention, and safe approximation remain open. The
single gateway, local tokens, demo approval service, and one connector do not
establish distributed, production, multi-engine, or live-agent behavior.

\section{Conclusion}

\system adds a stateful exposure boundary to task-scoped authorization: typed
release/dependency/outcome facts, same-snapshot evidence, exact pre-release novel-set
settlement, and one conserved root-family ledger. The finite model and
PostgreSQL campaigns support the declared safety properties and rewrite scope.
The outcome dimension closes the same-result threshold collapse while keeping
same-proposition replay free. The experiments also expose the design's present systems limit: million-fact exact replay
charges zero yet takes \RQFourScaleHighReplayS{}~s. Making that guarantee
streaming and compact, without weakening atomic budget safety, is the central
next step.

\bibliographystyle{IEEEtran}
\bibliography{references}

\end{document}
